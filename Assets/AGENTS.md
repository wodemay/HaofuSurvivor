# Repository Guidelines

## Project Structure & Module Organization

This directory is the Unity project's `Assets/` root. Put game code in `Scripts/`: gameplay belongs in `Scripts/Game/` and UI panels in `Scripts/UI/`. Scene assets are in `Scenes/`; art content is in `Art/`; render and Unity asset settings are in `Settings/`. `QFramework/` and `QFrameworkData/` contain the installed framework and its generated configuration; do not edit framework sources unless explicitly upgrading it.

Gameplay is organized around QFramework. Keep Unity-facing components and panels as controllers/views; express state in Models, domain behavior in Systems, mutations in Commands, read-only calculations in Queries, and cross-module updates as typed Events. Register application services through an `Architecture` rather than introducing unmanaged global state.

## Build, Test, and Development Commands

Open the Unity project root (the parent of `Assets/`) with the version recorded in `ProjectSettings/ProjectVersion.txt`. Use the Unity Editor to open `Scenes/SampleScene.unity` and enter Play mode for local verification. There is currently no committed CLI build or automated test suite; add Unity Test Framework tests before claiming automated coverage.

## Coding Style & Naming Conventions

Write C# with tabs, four-space visual width, braces on separate lines, and PascalCase for types, methods, public fields, and serialized fields. Use camelCase for parameters and local variables. Keep the `HaoFuSurvivor` namespace for project code. Name UI panels `UI<Name>Panel` and retain QFramework's paired partial files: hand-written behavior goes in `Name.cs`; generated bindings remain in `Name.Designer.cs` and must not be hand-edited.

Always unregister QFramework event subscriptions with the owning GameObject lifecycle, for example `.UnRegisterWhenGameObjectDestroyed(gameObject)`.

## Testing Guidelines

Place new Unity Test Framework tests under `Tests/` with `Editor/` or `PlayMode/` subfolders. Name test fixtures after the subject and tests as `Method_Condition_ExpectedResult`. Cover Commands, Queries, and Systems without scene dependencies where possible; use Play Mode tests for Unity integration.

## Commit & Pull Request Guidelines

No commit history exists yet. Use imperative, scoped messages such as `feat(ui): add pause panel` or `fix(player): stop movement on death`. Keep each commit focused. Pull requests should state the behavioral change, list affected scenes/assets, link the issue when available, and include screenshots or a short capture for visible UI or gameplay changes.

## Persistent Project Context

`AGENTS.md` is the repository's durable handoff document. After every agent-performed operation that changes code, assets, scenes, configuration, architecture decisions, validation status, or known issues, update this file in the same task. Record only material facts: what changed, why, affected paths, verification performed, and unresolved follow-up. Do not overwrite existing history; append a dated entry under **Change Log**. Keep entries concise enough to scan in one pass.

### Current State

- Project type: Unity 2D survivor game; current gameplay code is in `Scripts/Game/`, UI code in `Scripts/UI/`, and the active replacement scene is `Scenes/MainScene.unity`.
- Working-tree transition observed before player-control implementation: `Scenes/MainScene.unity` is the active replacement scene; legacy `SampleScene`, `Player`, `Enemy`, and Game Over UI files are pending removal. Preserve these user-originated changes unless a task explicitly addresses them.
- Framework decision (2026-07-30): QFramework is the mandatory core architecture for all new and refactored business logic. Enforce Controller -> Command -> System -> Model/Utility dependencies; use Query for reads and typed Event for notifications.
- UI workflow decision (2026-07-30): The user exclusively creates and edits UI panels, prefabs, GameObject hierarchies, controls, and QFramework UI bindings. When work requires any UI creation or structural UI change, stop before that step and wait for the user to confirm completion; then implement only the requested non-UI code integration.
- Existing examples: `Player` controls movement and opens `UIGameOverPanel` when its hurt box is triggered. `*.Designer.cs` files contain generated UI bindings.
- Design baseline: `Docs/GameArchitecture.md` and `Docs/GameArchitecture.zh-CN.md` define the planned QFramework modules for the 2D survivor game. Existing gameplay code is disposable and must not constrain the new architecture.
- Gameplay baseline: the first implemented slice is the QFramework player-control foundation. `GameStart` initializes `GameArchitecture` and opens the character-selection flow; `PlayerController` is a non-UI `Rigidbody2D` bridge that must be attached to the future player GameObject. Its current desktop adapter reads legacy `Horizontal`/`Vertical` axes; touch input waits for user-created controls.
- Camera baseline: `PlayerSpawnSystem` binds the selected player's transform to `CameraFollow` on the `MainCamera`-tagged camera after a successful spawn. `CameraFollow` is currently declared beside the already-imported `GameStart` type, tracks XY in `LateUpdate`, and preserves the camera's initial Z position; no scene or UI object was changed.
- Startup flow: `GameStart` initializes ResKit and `GameArchitecture`, then opens `UICharacterSelectPanel` through UIKit using its configured asset bundle. A confirmed choice persists the ID and dispatches `StartRunCommand`; the run does not start before confirmation.
- Run timer design: `RunTimelineConfig` at `Resources/Configs/RunTimeline.asset` defines ordered time-point events. `RunTimerSystem` advances only in `RunPhase.Active`, publishes elapsed time and one typed event per reached configuration stage, and exposes current enemy health/damage/move-speed/spawn-rate multipliers through `GetRunTimerStateQuery`. `UIGameHUDPanel` displays elapsed time; future enemy, spawn, and event modules must subscribe to `RunTimelineStageReachedEvent` or query this state.
- Character selection baseline: `CharacterConfig` ScriptableObject assets under `Resources/Configs/Characters/` provide the roles with numeric IDs 1 (survivor), 2 (vanguard), and 3 (scout). `CharacterCatalog` is the QFramework Utility that loads and orders these assets; `CharacterSelectionModel` stores only the selected integer ID, and `CharacterSelectionStorage` persists it with `PlayerPrefs`.
- `CharacterConfig.Id` and `CharacterConfig.SkillGroupId` are directly serialized integer fields. Skill-group ID 0 means no starting skill group; current survivor uses skill group 1, while vanguard and scout remain 0.
- All `CharacterConfig` columns use direct serialized names without `m` prefixes: `Id`, `DisplayName`, `SkillDescription`, `SkillGroupId`, `Icon`, `PlayerPrefab`, `SortOrder`, `MaxHealth`, `MoveSpeed`, and `AttackPower`.
- Character load contract: every `CharacterConfig` has a `PlayerPrefab` reference. The referenced gameplay prefab must contain `PlayerController` (which requires `Rigidbody2D`); `PlayerSpawnSystem` instantiates it at the origin after selection confirmation, then starts the run. Current mappings are survivor → `Square.prefab`, vanguard → `Triangle.prefab`, and scout → `Circle.prefab`.
- Temporary survivor spawn asset: `Character_Survivor` references `Art/Prefabs/Square.prefab`. Its root has `PlayerController` and a Kinematic `Rigidbody2D` with zero gravity; it is a temporary gameplay placeholder, not a final character prefab.
- Player-root contract: `Resources/Configs/PlayerRoot.asset` binds `Art/Prefabs/PlayerRoot.prefab` and `HealthBarCanvas.prefab`. `PlayerSpawnSystem` creates this root, ensures its movement components, parents each selected `CharacterConfig.PlayerPrefab` under `CharacterRoot`, and parents the health-bar canvas under `HealthBarAnchor`. The root prefab must retain both child names.
- Health-bar runtime contract: `PlayerHealthBarView` is added to each spawned health-bar canvas, reads the root player's QFramework health models, and refreshes its Slider on `PlayerDamagedEvent`. The user owns the canvas UI hierarchy and Slider binding.
- Enemy-root contract: `Resources/Configs/EnemyRoot.asset` binds `Art/Prefabs/EnemyRoot.prefab`. `EnemyFactory` creates this root per spawn, then parents the selected `EnemyConfig.Prefab` under `CharacterRoot`; the root prefab must retain that child name. `AttackRoot`, `EffectRoot`, `HurtBox`, and `DropRoot` are reserved for subsequent enemy systems.
- Enemy hierarchy and pooling: `Scenes/MainScene.unity` contains the active `EnemyContainer` root. EnemyRoot instances are parented there; `EnemyFactory` maintains a separate FIFO pool per enemy configuration ID, and `EnemySystem.Reset` returns active roots to that pool instead of destroying them.
- Pool safety contract: `EnemyFactory` and `ProjectileFactory` persist while their scene containers do not. Pool retrieval must discard Unity destroyed-object references after a scene transition. Enemy attack triggers use runtime ID 0 and must be re-registered on pooled-root activation rather than duplicated. Enemy movement is driven from GameStart.FixedUpdate through Rigidbody2D.MovePosition.
- Combat baseline: Player and Enemy use the same `AttackSystem`, `AttackCatalog`, `AttackConfig`, runtime registration commands, executor registry, CombatEntity faction identity, and generic DamageSystem entry point. `EnemyConfig.AttackIds` supplies an enemy's current attacks; a future player skill/attack module will register its selected attack IDs through the identical flow.
- Attack extension contract: `Resources/Configs/Combat/AttackCatalog.asset` owns all attack definitions. `AttackConfig.ExecutorId` selects a registered `IAttackExecutor`; no AttackType enum, target-faction field, or central attack-type branch exists. Standard attacks derive the opposing target faction from the runtime owner: Player attacks Enemy and Enemy attacks Player. ID 1001 uses executor `collision` and retains its configured damage/cooldown. Add projectile, special, and player capabilities as new executors and registration triggers.
- Projectile attack contract: attack ID 1002 uses executor `projectile` and `ProjectileAttackParameter_Projectile.asset`, which references user-created `Art/Prefabs/Attack/Bullet.prefab`. `ProjectileAttackTrigger` gets the nearest opposing CombatEntity through `FindClosestCombatTargetQuery`; `ProjectileFactory` pools bullet instances under a runtime-only `ProjectileContainer`. The parameter asset supplies immutable base speed, lifetime, range, and pool capacity; future upgrades must apply runtime modifiers by attack ID instead of mutating assets.
- Skill-group contract: `CharacterConfig.SkillGroupId` resolves through `SkillGroupCatalog` to `SkillGroupConfig`, which provides starting weapon IDs, skill IDs, and one dodge ID. `WeaponConfig` is a static Weapon container template with `InitialAttackIds`, level rules, and upgrade eligibility; EnemyConfig continues to use AttackIds directly. `PlayerLoadoutSystem` creates one `WeaponRuntimeData` per player weapon and owns weapon upgrade, Attack replacement, direct replacement, and reset. Upgrades never mutate ScriptableObject assets.
- Weapon evolution contract: `WeaponEvolutionCatalog.asset` maps a source Weapon ID and required level to a target Weapon ID. Evolution retains the original WeaponRuntime slot ID, removes its old attack triggers, sets the target weapon to level 1, and registers its initial attack contents. `EvolveWeaponCommand` rejects targets unless `MaxLevel = 1` and `CanUpgrade = false`, so evolved weapons cannot re-enter the upgrade pool. The catalog is intentionally empty until a real evolved WeaponConfig and Attack set are designed.
- Collision attack flow: EnemyFactory gives EnemyRoot CombatEntity faction identity, a Kinematic Rigidbody2D, and executor-defined trigger bridges. `CollisionAttackTrigger` resolves another CombatEntity's faction and sends generic register/try/unregister Commands; AttackSystem owns cooldown and invokes executor behavior. The current collision executor applies enemy timeline damage scaling before generic DamageSystem reaches PlayerSystem and the health bar. Pool release unregisters runtime state.
- Damage invulnerability baseline: PlayerStatModel exposes `DamageInvulnerabilityDuration`; PlayerModel tracks remaining time. The default duration is deliberately `0`, so current damage behavior is unchanged. Future upgrade logic may raise this stat by a small amount; PlayerSystem then ignores damage while the remaining time is positive.
- Enemy-health contract: `EnemyConfig.BaseHealth` is static base health. `EnemyFactory` registers each active EnemyRoot with `EnemyHealthSystem`, which applies the current `RunTimerModel.EnemyHealthMultiplier`, publishes damage/death events, and releases dead roots through `EnemySystem` back into the existing per-ID pool. All current enemy configurations use base health `20`; the survivor's projectile attack deals `10`, so a newly spawned opening-stage enemy dies in two hits before timeline health scaling.
- UI item implementation note: `UICharacterSelectItem` is an independent `MonoBehaviour` in `Scripts/UI/UICharacterSelectItem.cs`. It resolves the user-created item child nodes through `Transform.Find`; the panel owns only template cloning and selection flow.
- `UICharacterSelectItem` child paths: `Image_Select` and `Image_Icon` are direct children; `Text_Name` and `Text_Skill` are under the template's intermediate `GameObject` child and must be resolved as `GameObject/Text_Name` and `GameObject/Text_Skill`.
- `Scenes/TestUIPanels/TestUICharacterSelectPanel.unity` contains the UI root and `UICharacterSelectPanel`, but no `GameStart`. Run it additively after a boot scene, or add the existing `GameStart` component before using it as a standalone test scene; otherwise `GameArchitecture` is not initialized.
- Validation baseline: no committed automated tests or build scripts exist. The Git repository uses `main` and has an initial project commit.
- Git/PR workflow decision (2026-08-06): after every agent push, check whether an open PR exists from the pushed branch to `main`; create one when absent and report its URL, otherwise verify that the existing PR contains the pushed commit. The user performs review and merge on GitHub; do not merge without explicit authorization.

### Change Log

- 2026-08-07 | Added a non-functional change-log entry solely to refresh PR #5 for repository approval-permission verification. No gameplay code, asset, scene, or configuration behavior changed.
- 2026-08-07 | Submitted the current Assets worktree to `feat/unified-game-clock` as `cc0cd2e` (`fix(runtime): stabilize pause simulation`), including the HUD integration, unified-clock pause safeguards, and fixed-step post-pause correction. Push succeeded through the configured HTTP/1.1 proxy. Existing PR #5 is open against `main` and contains this commit: https://github.com/wodemay/HaofuSurvivor/pull/5. MCP package files under `Packages/` remain intentionally uncommitted.
- 2026-08-07 | Validated the fixed-step pause correction with `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers`: 0 errors. The existing MCPForUnity-related `System.Net.Http` and `System.IO.Compression` assembly-version warnings remain.
- 2026-08-07 | Fixed the post-pause physics jump in `Scripts/Game/GameStart.cs`: `TickRunPhysicsCommand` now receives `Time.fixedDeltaTime`, not `Time.fixedUnscaledDeltaTime`. The unscaled value reports wall-clock elapsed time since the previous physics tick and included pause duration on resume; the fixed simulation step prevents Player, Enemy, and Projectile movement from catching up after a pause. Preserved and corrected the user-added physics diagnostic log. Source compilation is pending.
- 2026-08-07 | Restored the HUD pause button as an explicit toggle: when the run is active it dispatches `PauseRunCommand`; when paused it dispatches `ResumeRunCommand`. This supersedes a brief incorrect idempotent-pause interpretation. All gameplay paths remain gated by `RunTimerSystem.IsRunning()` and Unity `Time.timeScale`; source compilation is pending this correction.

- 2026-07-30 | Added this contributor guide and durable project-context policy. Verified the Assets layout, QFramework installation, gameplay/UI scripts, scenes, and empty Git history.
- 2026-07-30 | Added root `.gitignore` for Unity-generated files, IDE files, and the QFramework package cache. Git remote: `https://github.com/wodemay/HaofuSurvivor.git`; the initial project submission is on `main` and excludes local generated content.
- 2026-07-30 | Created initial project commit `ba5dc5e` (2,392 files). Integrated the remote repository's initial `LICENSE` and `README.md` with merge commit `c813a91`; no conflicts occurred. A push to `origin/main` failed because this environment could not connect to `github.com:443`; retry `git push -u origin main` after network access is restored.
- 2026-07-30 | Added UI workflow boundary: UI creation and structural editing are performed by the user; agents pause and wait for confirmation before UI-dependent implementation.
- 2026-07-30 | Added `Docs/GameArchitecture.md`: the implementation blueprint for the PC/mobile single-player survivor game, including QFramework boundaries, runtime/meta modules, persistent data ownership, UI boundary, and delivery order. No gameplay or UI assets were changed.
- 2026-07-30 | Added `Docs/GameArchitecture.zh-CN.md`, a Chinese document synchronized with the English architecture blueprint. No gameplay or UI assets were changed.
- 2026-07-30 | Added player-control foundation under `Scripts/Architecture/` and `Scripts/Game/PlayerController.cs`: Run, Input, Player, Stats, Damage, commands, and typed gameplay events follow QFramework boundaries. `GameStart` now initializes the architecture and starts a run. No UI or scene objects were created; `PlayerController` awaits attachment to a user-created player GameObject with `Rigidbody2D`.
- 2026-07-30 | Command-line validation was blocked by a stale generated `Assembly-CSharp.csproj` that does not yet list the new scripts; it reported missing new types from `GameStart`. Open or refresh the project in Unity before the next build to regenerate the solution and perform source compilation.
- 2026-07-30 | Fixed `GameStart` architecture command dispatch: `IArchitecture.SendCommand` requires a command instance, so run startup now passes `new StartRunCommand()`.
- 2026-07-30 | Unity regenerated `Assembly-CSharp.csproj`; `dotnet build Assembly-CSharp.csproj --no-restore` now succeeds with 0 errors. Two pre-existing assembly reference-version warnings from MCPForUnity/Unity remain.
- 2026-07-30 | Integrated user-created `UICharacterSelectPanel` without changing its hierarchy or Bind fields. Added dynamic `UICharacterSelectItem` behavior using `Transform.Find`, character catalog/selection/persistence modules, selection commands/events, and player initialization from the selected character definition.
- 2026-07-30 | Unity did not synchronize a new Character source folder into the generated project even after metadata was added. Character module types are therefore colocated in the already-imported `GameArchitecture.cs`; their QFramework responsibilities and public contracts are unchanged.
- 2026-07-30 | Verified character-selection integration with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; the two existing MCPForUnity/Unity assembly reference-version warnings remain.
- 2026-07-30 | Inspected the user-created character-selection prefab: its item template and `CharacterGroup_Content` bindings match the runtime item-generation contract, and its item children match the required `Transform.Find` names. Revalidated with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; the two existing MCPForUnity/Unity assembly reference-version warnings remain. Identified that the standalone UI test scene has no `GameStart` bootstrap.
- 2026-07-31 | Moved `UICharacterSelectItem` into its own `Scripts/UI/UICharacterSelectItem.cs` file as the item controller. The existing panel, prefab hierarchy, and QFramework Bind fields were not changed. Refreshed Unity's asset database so the generated project includes the new script; Unity Console has 0 errors and `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Replaced the hardcoded character list with `CharacterConfig` ScriptableObject data. `UICharacterSelectItem`, player registration, and `CharacterCatalog` now consume the same configuration type. Added `Character_Survivor`, `Character_Vanguard`, and `Character_Scout` under `Resources/Configs/Characters/`; their `Icon` fields intentionally remain empty until character art exists. Verified Unity loads all three assets, and `dotnet build Assembly-CSharp.csproj --no-restore` completes with 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Connected the startup selection flow without changing UI assets: `GameStart` opens the user-created `UICharacterSelectPanel` via QFramework UIKit, and confirmation now starts the run after saving the selected character ID. Unity Console has 0 errors and `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Fixed the character item initialization null reference: the two text nodes are nested under the template's `GameObject` child rather than the item root. Updated their `Transform.Find` paths. Verified in Unity Editor by cloning the actual item template and calling `UICharacterSelectItem.Initialize` with `Character_Survivor`, which completed and returned ID `survivor`; `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Added `CharacterConfig.PlayerPrefab` and QFramework `PlayerSpawnSystem`. Confirming selection now dispatches `StartSelectedCharacterRunCommand`, which creates the configured gameplay prefab only when it includes `PlayerController`, then starts the run. The three initial configuration assets have no player prefab reference because no player prefab exists yet; real spawn validation requires user-created role prefabs. Unity Console has 0 errors and `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | `Character_Survivor.PlayerPrefab` already referenced the user-created `Art/Prefabs/Square.prefab`; added the missing root `PlayerController` and Kinematic `Rigidbody2D` components so the temporary survivor can spawn. Unity Editor asset verification confirms both components are present, and `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Added `CharacterConfig.SkillId` as the reserved static identifier for each role's future skill group. Existing configuration assets intentionally keep it empty until skill-group IDs are designed. `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain); Unity MCP refresh was unavailable because its server returned HTTP 502.
- 2026-07-31 | Converted character and skill identifiers from strings to integers across configuration, catalog, selection model/storage/commands/events, UI item callbacks, and player model. `CharacterConfig` now exposes directly serialized `Id` and `SkillId` fields; initial role IDs are 1/2/3 and all current skill IDs are 0. Confirmed the three player-prefab references map to Square/Triangle/Circle. `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Removed the remaining `m` prefixes from `CharacterConfig` serialized fields and migrated the three existing asset YAML fields without changing their content references or numerical values. `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Added the QFramework run-timer foundation, timeline configuration, time-stage multiplier/event contracts, remaining-time HUD integration, and time-limit victory state. `RunTimeline.asset` currently defines a provisional 20-minute schedule with stages at 2:00, 5:00, 10:00, 15:00, and 18:20; enemy/spawn systems are not implemented yet and must consume these published contracts. `dotnet build Assembly-CSharp.csproj --no-restore` succeeds with 0 errors; 2 pre-existing MCPForUnity/Unity reference-version warnings remain. Unity Play Mode verification is pending because the Unity MCP server is returning HTTP 502.
- 2026-07-31 | Fixed `RunTimerSystem` null references on run start and frame advance by registering `RunTimerModel` in `GameArchitecture`. `RunTimerSystem` can now resolve its QFramework model through the architecture lifecycle.
- 2026-07-31 | Converted the run timer from countdown to elapsed-time mode. Removed match-duration clamping and automatic victory; `RunTimeline.asset` now acts solely as a configurable list of time-point events, with each `EventId` published through `RunTimelineStageReachedEvent`. The existing HUD time binding now displays elapsed `MM:SS` without changing user-created UI objects.
- 2026-07-31 | Added runtime player-camera binding. After `PlayerSpawnSystem` validates and instantiates the selected role, it adds or reuses `CameraFollow` on the `MainCamera`-tagged camera and binds the player transform. The follow behavior runs in `LateUpdate` and retains the existing camera Z position.
- 2026-07-31 | Verified camera-follow integration with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; 2 pre-existing MCPForUnity/Unity reference-version warnings remain. `CameraFollow` was colocated in `GameStart.cs` because the current generated project file did not include a newly created standalone script.
- 2026-07-31 | Staged the complete current worktree, including the user-created scenes/UI assets and the QFramework gameplay foundation, for the `main` branch repository update.
- 2026-07-31 | Created local commit `e5ec607` (`feat(core): add survivor gameplay foundation`). Push to `origin/main` is pending: this environment could not connect to `github.com:443` while running `git push -u origin main`.
- 2026-08-07 | Pause architecture decision: a future pause feature will use the run timer/run state as the single gameplay execution gate. Attacks, player movement, enemy movement/spawning, damage timers, and other in-run simulation must stop when the timer is paused; Unity `Time.timeScale` may only be an auxiliary engine-level freeze, never the business-state source of truth. No pause code or UI was added in this task.
- 2026-08-03 | Added QFramework enemy spawning and pursuit foundation. `TickEnemiesCommand` advances `EnemySystem` after the run timer; it creates temporary red-square runtime enemies beyond the active camera bounds, moves them toward the registered player, and scales interval/wave size over elapsed time plus timeline multipliers. No UI or scene hierarchy was changed. Validation is pending.
- 2026-08-03 | Verified enemy-system integration with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; 2 pre-existing MCPForUnity/Unity reference-version warnings remain. Unity Play Mode verification remains pending.
- 2026-08-03 | Refactored mixed architecture declarations into separate Character and Enemy scripts. GameArchitecture now only registers modules; EnemyConfig at Resources/Configs/Enemy.asset owns spawn and movement values, and EnemyFactory is the persistent singleton responsible for prefab or temporary-placeholder creation. Unity must refresh the project before compiling the new scripts; the current generated .csproj is stale.
- 2026-08-03 | Integrated the user-created Player root and HealthBarCanvas without changing either prefab hierarchy. Added PlayerRoot configuration, root-based character spawning, runtime Rigidbody2D/PlayerController setup, health-bar instantiation at HealthBarAnchor, and QFramework PlayerDamagedEvent-to-Slider binding. The new source was validated with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; the two existing MCPForUnity/Unity reference-version warnings remain. Unity Play Mode verification is pending.
- 2026-08-04 | Renamed `Art/Prefabs/Player.prefab` to `PlayerRoot.prefab` and renamed its root GameObject; its GUID was retained, so PlayerRoot configuration references continue to resolve. Added EnemyRoot configuration and factory integration: every configured enemy now spawns as EnemyRoot with its visual/content prefab parented to CharacterRoot. No user-created hierarchy was changed. `dotnet build Assembly-CSharp.csproj --no-restore` succeeds with 0 errors; the two existing MCPForUnity/Unity reference-version warnings remain.
- 2026-08-04 | Added `EnemyContainer` as a saved MainScene root and converted EnemyFactory from direct instantiate/destroy to per-EnemyConfig-ID FIFO pooling. Spawned EnemyRoot objects are parented under EnemyContainer; a run reset deactivates and returns them to their matching pool. Verified source compilation with 0 errors; Unity-generated project synchronization now includes the new root-config scripts. Unity Play Mode pooling verification remains pending.
- 2026-08-04 | Configured Git global `http.proxy` and `https.proxy` as `http://127.0.0.1:17891` for repository network operations; both values were read back successfully.
- 2026-08-04 | Prepared the current gameplay, scene, prefab, configuration, architecture, and durable-context changes for the next GitHub submission on `main`.
- 2026-08-04 | Created local commit `427bfc2` (`feat(gameplay): add rooted enemy pooling`) containing the current project state. Initial push through the configured proxy failed with a Git TLS unexpected-EOF connection error; remote submission is being retried.
- 2026-08-04 | Retried the GitHub submission with HTTP/1.1 through the configured proxy. Push succeeded: `origin/main` now contains commits through `083eb63`.
- 2026-08-04 | Added repository-local Codex skill `.codex/skills/project-survivor-qframework`. It captures the QFramework boundaries, user-owned UI workflow, root-prefab/configuration contracts, enemy pooling, validation, Git, and AGENTS handoff requirements for future sessions.
- 2026-08-04 | Prepared the validated repository-local ProjectSurvivor Codex skill and its durable-context update for local Git commit.
- 2026-08-04 | Local skill commit `3a70dd1` was not included in the preceding GitHub push; the branch is now being pushed so `.codex/skills/project-survivor-qframework` becomes available remotely.
- 2026-08-04 | GitHub push succeeded through `e6d0251`; the repository now contains `.codex/skills/project-survivor-qframework`.
- 2026-08-04 | Implemented the first configurable enemy attack path: all four current enemy configurations now reference contact attack ID 1001. Added attack catalog/configuration, QFramework attack system/commands, EnemyRoot trigger bridge and runtime Rigidbody2D setup; contact damage obeys its configured cooldown and the run damage multiplier, then uses the existing player-damage/health-bar flow. Source compilation succeeds with 0 errors; the two pre-existing MCPForUnity/Unity reference-version warnings remain. Unity Play Mode contact verification is pending.
- 2026-08-04 | Updated the repository-local ProjectSurvivor Codex skill with the configurable enemy attack contract and the current contact-attack runtime flow.
- 2026-08-04 | Prepared the configurable enemy contact-attack implementation, configuration assets, skill update, and durable-context changes for GitHub submission on `main`.
- 2026-08-04 | Added upgrade-ready player damage invulnerability. Default duration is 0 seconds; PlayerSystem now starts and ticks the duration only after future upgrades increase PlayerStatModel.DamageInvulnerabilityDuration. Verified with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; the two existing MCPForUnity/Unity reference-version warnings remain. Updated the project Skill with this contract.
- 2026-08-04 | Replaced the enemy-only attack module with the shared Combat architecture. Removed EnemyAttackConfig/Catalog/System, EnemyContactAttack, AttackType, and their legacy assets. Added generic attack configuration/catalog, runtime/system/commands, executor registry, CombatEntity factions, and ContactAttackTrigger; the existing enemy contact attack remains functional through ExecutorId `contact`. PlayerRoot and EnemyRoot now receive generic combat identities, so later player skills and enemy projectile/special behavior use the same contracts. Verified source compilation with 0 errors; 2 existing MCPForUnity/Unity reference-version warnings remain. Unity Play Mode verification is pending.
- 2026-08-04 | Prepared the shared Combat refactor, legacy enemy-attack cleanup, configuration migration, project Skill update, and durable-context changes for GitHub submission on `main`.
- 2026-08-04 | Created and pushed commit `a38c113` (`refactor(combat): unify attack execution`) to `origin/main`; the shared Combat architecture and removal of legacy enemy-only attack code are available on GitHub.
- 2026-08-05 | Added Chinese extension guides under `Docs/Guides/`: `NewAttack.zh-CN.md`, `NewEnemy.zh-CN.md`, and `NewCharacter.zh-CN.md`. They define the current Combat ExecutorId workflow and data/root contracts for safely adding attacks, enemies, and characters.
- 2026-08-05 | Added `Scripts/Editor/ConfigurationCreatorWindow.cs`, available at Unity menu `ProjectSurvivor/Configuration Creator`. It creates CharacterConfig or EnemyConfig with the next unused numeric ID, can create an empty content-prefab template, and automatically appends new enemies to EnemyCatalog. It never creates PlayerRoot, EnemyRoot, gameplay UI, or artwork. Updated extension and script-reference docs accordingly. `dotnet build Assembly-CSharp-Editor.csproj --no-restore` succeeds with 0 errors; 2 existing MCPForUnity/Unity reference-version warnings remain. Interactive Unity Editor verification is pending.
- 2026-08-05 | Updated the repository-local ProjectSurvivor Codex skill with the Configuration Creator contract and its explicit scope boundaries.
- 2026-08-05 | Expanded `Docs/Guides/NewAttack.zh-CN.md` into a step-by-step Chinese integration manual. It now specifies Unity folder/menu/Inspector operations, AttackCatalog registration, Enemy assignment, executor and trigger code locations, generic command calls, future executor-parameter configuration, and validation requirements.
- 2026-08-05 | Corrected the new-attack naming rule in `NewAttack.zh-CN.md`: use `Attack_<技能名称>` because attack configurations are reusable and must not encode a particular Character, Enemy, or usage context.
- 2026-08-05 | Prepared the Configuration Creator editor, complete Chinese documentation set, ProjectSurvivor skill updates, and durable project context for GitHub submission on branch `feat/config-editor`.
- 2026-08-05 | Created and pushed `e8ea068` (`feat(editor): add configuration creator`) to `origin/feat/config-editor`. The remote branch now contains the Configuration Creator, all Docs additions, skill updates, and this project context; it is not merged into `main`.
- 2026-08-05 | Automated PR creation for `feat/config-editor` is blocked because GitHub CLI (`gh`) is not installed in this environment. The branch is fully pushed; create a PR manually from GitHub's branch comparison page when ready to merge it into `main`.
- 2026-08-05 | Located GitHub CLI at `C:\Program Files\GitHub CLI\gh.exe` (v2.92.0) and created PR #1 for `feat/config-editor` into `main`: `https://github.com/wodemay/HaofuSurvivor/pull/1`. `gh auth status` still reports its keyring token as invalid; re-authenticate with `gh auth login -h github.com` if subsequent GitHub CLI operations fail.
- 2026-08-04 | Added `Docs/Scripts/ScriptReference.zh-CN.md`: a Chinese, per-script reference for all current project scripts, grouped by module and documenting QFramework responsibilities, generated-code boundaries, and maintenance rules.
- 2026-08-04 | Created and pushed commit `8cb56e0` (`feat(enemy): add contact attack flow`) to `origin/main`; the contact-attack implementation and updated project Skill are available on GitHub.
- 2026-08-05 | Renamed the current shared collision attack executor ID from `contact` to `collision`. Renamed the bridge script and configuration asset to `CollisionAttackTrigger` and `Attack_Collision`; retained attack ID 1001 and its AttackCatalog reference. Documentation and the local project Skill were synchronized. Unity MCP refresh reported 0 Console errors; `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors and 2 pre-existing assembly-version warnings.
- 2026-08-05 | Removed `AttackConfig.TargetFaction` from the shared Combat contract and both current attack assets. `AttackSystem` now rejects same-faction targets and derives standard attack targeting from the runtime owner faction, so a single configuration can be used by Player or Enemy. Documentation and the local project Skill were synchronized. Unity MCP refresh reported 0 Console errors; `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors and 2 pre-existing assembly-version warnings.
- 2026-08-05 | Added generic projectile-attack foundation on branch `feat/projectile-attack`. `Attack_Projectile` (ID 1002) now references `ProjectileAttackParameter_Projectile` and the user-created Bullet prefab; added projectile executor, targeting query/system, trigger, pooled runtime factory, controller, and object-based damage command. Attack execution now retains concrete owner/target objects, while same-faction checks remain centralized in AttackSystem. Added `Docs/Guides/ProjectileAttack.zh-CN.md`. No EnemyConfig or CharacterConfig was assigned ID 1002, so the behavior is ready but not equipped until a user-selected Enemy attack list or the future SkillGroupId-to-AttackIds module assigns it. `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors and 2 pre-existing assembly-version warnings; Unity MCP Console has 0 errors. Enemy health/damage resolution remains a future module.
- 2026-08-05 | Renamed `CharacterConfig.SkillId` to `SkillGroupId` and migrated all three character assets without changing their value (`0`). The Configuration Creator label and Chinese documentation now use the same group-level name. Unity MCP Console has 0 errors; `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors and 2 pre-existing assembly-version warnings.
- 2026-08-05 | Added the player skill-group foundation. `SkillGroupConfig` now provides starting weapon, skill, and dodge IDs; `WeaponConfig` maps weapons to generic attacks. `PlayerLoadoutSystem` loads `CharacterConfig.SkillGroupId` after player creation, equips weapon attacks, stores skill/dodge IDs, and resets on player destruction. Added projectile starter group ID 1 and weapon ID 1, and assigned it to Character_Survivor; skills and dodge intentionally remain unimplemented data contracts. Added `Docs/Guides/SkillGroup.zh-CN.md`. Unity MCP Console has 0 errors; `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors and 2 pre-existing assembly-version warnings. A temporary hidden Editor object verified group 1 equips weapon 1 and adds `ProjectileAttackTrigger`; no scene changes were saved.
- 2026-08-05 | Added QFramework enemy-health resolution. `EnemyConfig.BaseHealth` was added and set to `20` for the four current enemy assets; `EnemyHealthSystem` registers spawned EnemyRoots, applies timeline health scaling, emits enemy damage/death events, and returns killed enemies through the existing EnemySystem/EnemyFactory pool flow. `DamageSystem` now routes Enemy targets to this system. With projectile attack ID 1002 dealing `10`, opening-stage enemies require two hits. Unity MCP Console reported 0 errors and `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors; the 2 existing MCPForUnity/Unity assembly-version warnings remain.
- 2026-08-05 | Created and pushed `feat/projectile-attack` commit `5d4709d` (`feat(combat): add projectile attack and loadout`). Opened PR #2 into `main`: `https://github.com/wodemay/HaofuSurvivor/pull/2`. The PR includes the shared projectile combat, player skill-group/loadout, collision naming, enemy-health/two-hit opening calibration, documentation, and durable-context updates. GitHub CLI required `HTTP_PROXY` and `HTTPS_PROXY` set to `http://127.0.0.1:17891` for API access.
- 2026-08-06 | Rebuilt `Docs/Scripts/ScriptReference.zh-CN.md` from the current `Assets/Scripts/` inventory. It now lists every script by module, corrects the shared combat/projectile/skill-group/enemy-health responsibilities, identifies generated Bind files and the `CameraFollow` colocated type, and records maintenance boundaries. No gameplay assets or code changed.
- 2026-08-06 | Added `Docs/Guides/AttackFlow.zh-CN.md`, documenting the current QFramework attack flow for player and enemy owners: configuration origin, runtime registration, targeting, cooldown, executor behavior, damage routing, death, and pool cleanup. It records that attacks are shared by owner faction and executor, not duplicated by actor type. No gameplay assets or code changed.
- 2026-08-06 | Updated the global Codex `add-feature` skill at `C:\Users\Administrator\.codex\skills\add-feature\SKILL.md`: sections 4 and 5 now use a Unity workflow (QFramework boundaries, assets/meta validation, Unity MCP refresh/Console checks, `dotnet build`, Unity Test Framework, and Play Mode lifecycle checks) instead of the former web/npm workflow. Validated with `python -X utf8 ...\quick_validate.py`; no project gameplay files changed.
- 2026-08-06 | Refactored player weapons into runtime Attack containers. WeaponConfig now supplies `InitialAttackIds`, `MaxLevel`, `CanUpgrade`, and per-level Attack add/remove data; PlayerLoadoutModel now owns distinct WeaponRuntimeData slots. PlayerLoadoutSystem supports equip, level upgrade, Attack-content replacement, direct Weapon replacement, configured evolution, cleanup, typed weapon events, commands, and a read-only query. Attack triggers now track a WeaponRuntime ID, allowing independent triggers for separate Weapon containers while EnemyConfig remains direct AttackIds. Added empty `WeaponEvolutionCatalog.asset`; real evolution targets must be user-configured as level-1 non-upgradeable WeaponConfigs. Unity MCP temporary unsaved-object validation passed initial equip, level upgrade, Attack replacement, direct replacement, and configured evolution to a level-1 non-upgradeable target. `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` has 0 errors with 2 pre-existing assembly-version warnings. Unity MCP's console response retains five historical edit-mode `Destroy` logs produced before AttackTrigger cleanup was corrected to use `DestroyImmediate`; the corrected validation rerun produced no new logs. Updated Weapon, skill-group, attack-flow, script-reference, and project-skill documentation.
- 2026-08-06 | Created and pushed commit `70ea2a5` (`refactor(weapon): model weapons as runtime attack containers`) to `feat/projectile-attack`; existing PR #2 is updated at `https://github.com/wodemay/HaofuSurvivor/pull/2`. The two MCPForUnity/Unity assembly-version warnings are accepted as existing environment warnings.
- 2026-08-06 | Recorded the Git/PR workflow: every push must be followed by an open-PR check for that branch to `main`; create and report a PR if absent, otherwise confirm the existing PR update. User review and merge remain GitHub-side actions.
- 2026-08-07 | Refined `Docs/Scripts/ScriptReference.zh-CN.md` for the runtime Weapon-container refactor: documented WeaponConfig level fields, stable runtime slots, evolution constraints, Attack Trigger ownership/lifecycle, and weapon Commands/Events. No gameplay assets or code changed.
- 2026-08-07 | Fixed pooled combat lifecycle defects: EnemyRoot attack triggers with runtime ID 0 are reused on activation instead of duplicating components; EnemyFactory and ProjectileFactory discard destroyed pooled references after scene changes; player spawn now aborts and unregisters when initial skill-group equipment fails; enemy ticking moved to FixedUpdate and uses Rigidbody2D.MovePosition. Unity Play Mode temporary validation confirmed a pooled enemy retains one collision trigger after reuse. Unity MCP Console has 0 errors and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` has 0 errors; 2 existing assembly-version warnings remain. Updated script reference, attack flow, and project Skill.
- 2026-08-07 | Added the unified run-time foundation on `feat/unified-game-clock`. `RunTimerSystem` is now the sole source of gameplay frame and physics deltas; player/enemy/projectile movement, projectile lifetime, attack cooldowns, and player invulnerability consume its output rather than independently reading Unity time. Added paused state, pause/resume commands/events, and `GetRunTimeStateQuery`; `Time.timeScale` is synchronized only by the run timer as an engine-level freeze. No UI was created or changed. `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` has 0 errors; the 2 MCPForUnity assembly-version warnings remain. Unity MCP refresh did not return before its timeout, so Unity Console and Play Mode verification remain pending.
- 2026-08-07 | Pushed `79f39ce` to `feat/unified-game-clock` and opened PR #4 into `main`: `https://github.com/wodemay/HaofuSurvivor/pull/4`. `Packages/manifest.json`, `Packages/packages-lock.json`, and the untracked `Packages/unity-mcp-beta/` directory were intentionally excluded because they are user-originated MCP package changes.
- 2026-08-07 | Added a non-functional unified-time maintenance note solely to validate the existing-PR push workflow. It does not change gameplay behavior, assets, or configuration.
- 2026-08-07 | Pushed `353959d` to `feat/unified-game-clock`. PR #4 was no longer open, so created replacement PR #5 into `main`: `https://github.com/wodemay/HaofuSurvivor/pull/5`. GitHub CLI GraphQL intermittently returned EOF; REST API verified the replacement PR is open and points to commit `353959d`.
- 2026-08-07 | Integrated the user-created UIGameHUDPanel controls without changing its hierarchy or Bind fields. Button_Pause now sends PauseRunCommand. Button_Back temporarily exits the active run through ExitRunToCharacterSelectionCommand, stops the timer, returns active enemies to the pool, destroys the current PlayerRoot, closes the HUD, and opens UICharacterSelectPanel; this is explicitly temporary until the final return flow is designed. dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers completed with 0 errors; existing framework/MCP warnings remain.
- 2026-08-07 | Fixed HUD pause and restart-run defects. Button_Pause now toggles PauseRunCommand and ResumeRunCommand from the unified run-time state. EnemySystem.Reset now ignores destroyed entries and invokes EnemyFactory.ReleaseAllActive as a factory-level fallback, preventing active EnemyRoots missed by the system list from surviving an exit/restart. dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers completed with 0 errors; 2 existing MCP assembly-version warnings remain. git diff --check reports only user-created UIGameHUDPanel prefab YAML trailing whitespace.
- 2026-08-07 | Verified Unity MCP HTTP transport is already running: `mcp-for-unity.exe` (Python PID 24836) connects to `http://127.0.0.1:8800` with `--project-scoped-tools`, and Unity owns the paired connection. The Codex-side `unityMCP` resource server still reports not ready, so MCP resource/tool validation cannot proceed until that registration becomes ready; do not start a duplicate transport process.
- 2026-08-07 | Hardened pause behavior so simulation cannot continue behind a frozen presentation. After the timer tick, GameStart skips gameplay and physics dispatch while paused; PlayerController, collision/projectile attack triggers, projectile hit handling, and DamageSystem each enforce the same run-time gate. Paused input is cleared to prevent stale movement on resume. `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` completed with 0 errors; 2 existing MCP assembly-version warnings remain. `git diff --check` reports only user-created UIGameHUDPanel prefab YAML trailing whitespace.
