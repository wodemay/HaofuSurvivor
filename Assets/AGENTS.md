# Repository Guidelines

## Project Structure & Module Organization

This directory is the Unity project's `Assets/` root. Put game code in `Scripts/`: gameplay belongs in `Scripts/Game/` and UI panels in `Scripts/UI/`. Scene assets are in `Scenes/`; art content is in `Art/`; render and Unity asset settings are in `Settings/`. `QFramework/` and `QFrameworkData/` contain the installed framework and its generated configuration; do not edit framework sources unless explicitly upgrading it.

Gameplay is organized around QFramework. Keep Unity-facing components and panels as controllers/views; express state in Models, domain behavior in Systems, mutations in Commands, read-only calculations in Queries, and cross-module updates as typed Events. Register application services through an `Architecture` rather than introducing unmanaged global state.

## Build, Test, and Development Commands

Open the Unity project root (the parent of `Assets/`) with the version recorded in `ProjectSettings/ProjectVersion.txt`. Use the Unity Editor to open `Scenes/SampleScene.unity` and enter Play mode for local verification. There is currently no committed CLI build or automated test suite; add Unity Test Framework tests before claiming automated coverage.

## Coding Style & Naming Conventions

Write C# with tabs, four-space visual width, braces on separate lines, and PascalCase for types, methods, public fields, and serialized fields. Use camelCase for parameters and local variables. Keep the `HaoFuSurvivor` namespace for project code. Name UI panels `UI<Name>Panel` and retain QFramework's paired partial files: hand-written behavior goes in `Name.cs`; generated bindings remain in `Name.Designer.cs` and must not be hand-edited.

Always unregister QFramework event subscriptions with the owning GameObject lifecycle, for example `.UnRegisterWhenGameObjectDestroyed(gameObject)`.

## Testing Guidelines

Place new Unity Test Framework tests under `Tests/` with `Editor/` or `PlayMode/` subfolders. Name test fixtures after the subject and tests as `Method_Condition_ExpectedResult`. Cover Commands, Queries, and Systems without scene dependencies where possible; use Play Mode tests for Unity integration.

## Commit & Pull Request Guidelines

No commit history exists yet. Use imperative, scoped messages such as `feat(ui): add pause panel` or `fix(player): stop movement on death`. Keep each commit focused. Pull requests should state the behavioral change, list affected scenes/assets, link the issue when available, and include screenshots or a short capture for visible UI or gameplay changes.

## Persistent Project Context

`AGENTS.md` is the repository's durable handoff document. After every agent-performed operation that changes code, assets, scenes, configuration, architecture decisions, validation status, or known issues, update this file in the same task. Record only material facts: what changed, why, affected paths, verification performed, and unresolved follow-up. Do not overwrite existing history; append a dated entry under **Change Log**. Keep entries concise enough to scan in one pass.

Before every agent run or task action in this repository, read this entire `AGENTS.md` file first. Treat it as mandatory execution context, not optional reference material.

After every code change, perform a focused code review before validation, commit, push, or handoff. Review changed paths for compilation errors, QFramework boundary violations, lifecycle and pooling defects, null references, and unintended UI/prefab modifications; fix all findings before proceeding.

Write all project documentation in the project-root `Docs/` directory, never under `Assets/Docs/`.

### Current State

- Project type: Unity 2D survivor game; current gameplay code is in `Scripts/Game/`, UI code in `Scripts/UI/`, and the active replacement scene is `Scenes/MainScene.unity`.
- Working-tree transition observed before player-control implementation: `Scenes/MainScene.unity` is the active replacement scene; legacy `SampleScene`, `Player`, `Enemy`, and Game Over UI files are pending removal. Preserve these user-originated changes unless a task explicitly addresses them.
- Framework decision (2026-07-30): QFramework is the mandatory core architecture for all new and refactored business logic. Enforce Controller -> Command -> System -> Model/Utility dependencies; use Query for reads and typed Event for notifications.
- UI workflow decision (2026-07-30): The user exclusively creates and edits UI panels, prefabs, GameObject hierarchies, controls, and QFramework UI bindings. When work requires any UI creation or structural UI change, stop before that step and wait for the user to confirm completion; then implement only the requested non-UI code integration.
- Existing examples: `Player` controls movement and opens `UIGameOverPanel` when its hurt box is triggered. `*.Designer.cs` files contain generated UI bindings.
- Design baseline: `Docs/GameArchitecture.md` and `Docs/GameArchitecture.zh-CN.md` define the planned QFramework modules for the 2D survivor game. Existing gameplay code is disposable and must not constrain the new architecture.
- Gameplay baseline: `GameStart` initializes `GameArchitecture` and opens the main menu. `PlayerController` is retained only as an empty compatibility marker for existing character-content prefabs; input and movement belong to `InputSystem` and `PlayerSystem`.
- Camera baseline: `PlayerSpawnSystem` binds the selected player's transform to `CameraFollow` on the `MainCamera`-tagged camera after a successful spawn. `CameraFollow` is an independent presentation-only script that tracks XY in `LateUpdate` and preserves the camera's initial Z position.
- Startup flow: `GameStart` initializes ResKit and `GameArchitecture`, then opens `UIMainMenuPanel`. Start enters `UICharacterSelectPanel`; a confirmed choice persists the ID and dispatches `StartSelectedCharacterRunCommand`; the run does not start before confirmation.
- Directory layout: QFramework business code remains in `Scripts/Architecture/<Module>/`; Unity bridges are split into `Scripts/Game/Bootstrap`, `Camera`, `Combat`, `Player`, and presentation-only `Presentation`. Runtime Configs mirror their owner module: `Character`, `Enemy`, `Player`, `Combat/Attack|Weapon|Skill|Dodge`, `Progression/Experience`, and `Run`. See `Docs/Guides/DirectoryLayout.zh-CN.md` before adding a new script or Config asset.
- Runtime clock contract: `RunTimelineConfig` at `Resources/Configs/Run/RunTimeline.asset` defines ordered time-point events. `RunTimerSystem` is the only gameplay-time source; `GameLoopSystem` advances it then dispatches only registered `IRunUpdateable` / `IRunFixedUpdateable` Systems while `RunPhase.Active`. `GameStart` is the only gameplay Update/FixedUpdate host. Paused and level-up states dispatch no gameplay ticks. `UIGameHUDPanel` displays elapsed time; future enemy, spawn, and event modules must subscribe to `RunTimelineStageReachedEvent` or query this state.
- Character selection baseline: `CharacterConfig` ScriptableObject assets under `Resources/Configs/Character/` provide the roles with numeric IDs 1 (survivor), 2 (vanguard), and 3 (scout). `CharacterCatalog` is the QFramework Utility that loads and orders these assets; `CharacterSelectionModel` stores only the selected integer ID, and `CharacterSelectionStorage` persists it with `PlayerPrefs`.
- `CharacterConfig.Id` and `CharacterConfig.SkillGroupId` are directly serialized integer fields. Skill-group ID 0 means no starting skill group; current survivor uses skill group 1, while vanguard and scout remain 0.
- All `CharacterConfig` columns use direct serialized names without `m` prefixes: `Id`, `DisplayName`, `SkillDescription`, `SkillGroupId`, `Icon`, `PlayerPrefab`, `SortOrder`, `MaxHealth`, `MoveSpeed`, and `AttackPower`.
- Character load contract: every `CharacterConfig` has a `PlayerPrefab` reference. It is content-only; `PlayerSpawnSystem` instantiates it under PlayerRoot's `CharacterRoot`, removes any legacy `PlayerController`, and PlayerRoot owns runtime Rigidbody2D, CombatEntity and movement. Current mappings are survivor, vanguard and scout placeholder prefabs.
- Temporary survivor spawn asset: `Character_Survivor` references `Art/Prefabs/Square.prefab`. Its root has `PlayerController` and a Kinematic `Rigidbody2D` with zero gravity; it is a temporary gameplay placeholder, not a final character prefab.
- Player-root contract: `Resources/Configs/Player/PlayerRoot.asset` binds `Art/Prefabs/PlayerRoot.prefab` and `HealthBarCanvas.prefab`. `PlayerSpawnSystem` creates this root, ensures its movement components, parents each selected `CharacterConfig.PlayerPrefab` under `CharacterRoot`, and parents the health-bar canvas under `HealthBarAnchor`. The root prefab must retain both child names.
- Health-bar runtime contract: `PlayerHealthBarView` is added to each spawned health-bar canvas, reads the root player's QFramework health models, and refreshes its Slider on `PlayerDamagedEvent`. The user owns the canvas UI hierarchy and Slider binding.
- Enemy-root contract: `Resources/Configs/Enemy/EnemyRoot.asset` binds `Art/Prefabs/EnemyRoot.prefab`. `EnemyFactory` creates this root per spawn, then parents the selected `EnemyConfig.Prefab` under `CharacterRoot`; the root prefab must retain that child name. `AttackRoot`, `EffectRoot`, `HurtBox`, and `DropRoot` are reserved for subsequent enemy systems.
- Enemy hierarchy and pooling: MainScene WorldRoot/EnemyRoot is the sole EnemyRoot pool parent. EnemyFactory maintains a separate FIFO pool per enemy configuration ID, and EnemySystem.Reset returns active roots to that pool instead of destroying them.- Pool safety contract: `EnemyFactory` and `ProjectileFactory` persist while their scene containers do not. Pool retrieval must discard Unity destroyed-object references after a scene transition. Enemy attack triggers use runtime ID 0 and must be re-registered on pooled-root activation rather than duplicated. Enemy movement is driven by `EnemySystem` through `GameLoopSystem` fixed ticks and Rigidbody2D.MovePosition.
- Combat baseline: Player and Enemy use the same `AttackSystem`, `AttackCatalog`, `AttackConfig`, runtime registration commands, executor registry, CombatEntity faction identity, and generic DamageSystem entry point. `EnemyConfig.AttackIds` supplies an enemy's current attacks; a future player skill/attack module will register its selected attack IDs through the identical flow.
- Attack extension contract: `Resources/Configs/Combat/Attack/AttackCatalog.asset` owns all attack definitions. `AttackConfig.ExecutorId` selects a registered `IAttackExecutor`; no AttackType enum, target-faction field, or central attack-type branch exists. Standard attacks derive the opposing target faction from the runtime owner: Player attacks Enemy and Enemy attacks Player. ID 1001 uses executor `collision` and retains its configured damage/cooldown. Add projectile, special, and player capabilities as new executors and registration triggers.
- Projectile attack contract: attack ID 1002 uses executor projectile and ProjectileAttackParameter_Projectile.asset. ProjectileFactory pools instances by immutable parameter asset under MainScene WorldRoot/ProjectileRoot; base values remain in parameter assets and runtime upgrades do not mutate assets.- Survivor skill baseline: Skill ID 1 uses Attack ID 1004 (`barrage-projectile`) as a manual, targetless ability. `InputSystem` maps Space to `RequestSkillCommand`; `PlayerLoadoutSystem` resolves equipped skill runtimes and `AttackSystem` enforces its 15-second cooldown. `BarrageProjectileSystem` immediately starts a three-second emission circle that rotates clockwise around the player, picks random points inside that circle every 0.025 seconds, and fires each projectile outward along the player-to-circle radius at speed 16. It is never auto-fired and remains frozen outside `RunPhase.Active`.
- Skill-group contract: `CharacterConfig.SkillGroupId` resolves through `SkillGroupCatalog` to `SkillGroupConfig`, which provides starting weapon IDs, skill IDs, and one dodge ID. `WeaponConfig` is a static Weapon container template with `InitialAttackIds`, level rules, and upgrade eligibility; EnemyConfig continues to use AttackIds directly. `PlayerLoadoutSystem` creates one `WeaponRuntimeData` per player weapon and owns weapon upgrade, Attack replacement, direct replacement, and reset. Upgrades never mutate ScriptableObject assets.
- Weapon-combination contract: `WeaponCombinationCatalog.asset` owns all final-form recipes. A recipe defines required Weapon IDs/minimum levels, optional stat-upgrade IDs/minimum levels, a `CandidateChance`, and a target Weapon. At each level-up, successful combination rolls occupy priority candidate slots. One-source recipes replace the source runtime slot; multi-source recipes release every source attack, remove all source slots, then add one target slot. Attributes are only checked, never consumed. Targets must be level 1 and non-upgradeable. Current recipes are Weapon 1 Lv5 + Stat 1 -> Weapon 2 and Weapon 3 Lv5 + Stat 2 -> Weapon 4.
- Inferno FireBall contract: Weapon 4 uses Attack 1006 with executor `explosive-projectile`, the moved `Art/Prefabs/Attack/FireBall/FireBall.prefab`, and user-created `FireBallExplosion.prefab` / `FireGroundFlame.prefab` in that folder. The impact command reaches `ExplosiveAreaSystem`, which applies immediate range damage and schedules ground-fire ticks exclusively through `GameLoopSystem`; `AreaEffectFactory` pools visual effects. No gameplay logic relies on visual Prefab colliders or Update methods.
- Collision attack flow: EnemyFactory gives EnemyRoot CombatEntity faction identity, a Kinematic Rigidbody2D, and executor-defined trigger bridges. `CollisionAttackTrigger` resolves another CombatEntity's faction and sends generic register/try/unregister Commands; AttackSystem owns cooldown and invokes executor behavior. The current collision executor applies enemy timeline damage scaling before generic DamageSystem reaches PlayerSystem and the health bar. Pool release unregisters runtime state.
- Damage invulnerability baseline: PlayerStatModel exposes `DamageInvulnerabilityDuration`; PlayerModel tracks remaining time. The default duration is deliberately `0`, so current damage behavior is unchanged. Future upgrade logic may raise this stat by a small amount; PlayerSystem then ignores damage while the remaining time is positive.
- Enemy-health contract: `EnemyConfig.BaseHealth` is static base health. `EnemyFactory` registers each active EnemyRoot with `EnemyHealthSystem`, which applies the current `RunTimerModel.EnemyHealthMultiplier`, publishes damage/death events, and releases dead roots through `EnemySystem` back into the existing per-ID pool. All current enemy configurations use base health `20`; the survivor's projectile attack deals `10`, so a newly spawned opening-stage enemy dies in two hits before timeline health scaling.
- Experience-drop baseline: EnemyDiedEvent carries immutable drop configuration and death position before pooling; ExperienceSystem pools ExperienceOrb instances under MainScene WorldRoot/PickupRoot. Captured drops accelerate toward the player until collected, using RunTimerModel time.- Level-up baseline: `ExperienceSystem` ends at `PlayerLevelUpEvent`; it has no dependency on weapon upgrades, level-up UI, or RunPhase. `LevelUpSystem` owns a resettable queue of level-selection requests, current weapon-option query, selection confirmation, and `RunPhase.LevelUpSelection` entry/exit. `UILevelUpPanel` reads LevelUp queries and sends its confirmation Command. This initial slice offers only existing upgradeable Weapons; adding new Weapons, Skills, dodge upgrades, selection weights, and drop tables remains future work.
- Unity MCP status (2026-08-10): HTTP MCP is connected to `ProjectSurvivor@662e2df93b4f2899` (Unity 2022.3.62f3c1). MainScene is idle and not compiling; Console error query currently returns 0 entries. MCP Console and Play Mode validation are available again.
- UI item implementation note: `UICharacterSelectItem` is an independent `MonoBehaviour` in `Scripts/UI/UICharacterSelectItem.cs`. It resolves the user-created item child nodes through `Transform.Find`; the panel owns only template cloning and selection flow.
- `UICharacterSelectItem` child paths: `Image_Select` and `Image_Icon` are direct children; `Text_Name` and `Text_Skill` are under the template's intermediate `GameObject` child and must be resolved as `GameObject/Text_Name` and `GameObject/Text_Skill`.
- `Scenes/TestUIPanels/TestUICharacterSelectPanel.unity` contains the UI root and `UICharacterSelectPanel`, but no `GameStart`. Run it additively after a boot scene, or add the existing `GameStart` component before using it as a standalone test scene; otherwise `GameArchitecture` is not initialized.
- Validation baseline: no committed automated tests or build scripts exist. The Git repository uses `main` and has an initial project commit.
- Git/PR workflow decision (2026-08-06): after every agent push, check whether an open PR exists from the pushed branch to `main`; create one when absent and report its URL, otherwise verify that the existing PR contains the pushed commit. The user performs review and merge on GitHub; do not merge without explicit authorization.

### Change Log

- 2026-08-20 | 修复开放网格地图覆盖玩家/敌人的渲染问题：根节点 `WorldRoot/MapBackgroundRoot` 层级正确，实际原因是当前 TagManager 仅有 `Default` 与 `MapBackground`，地面层排序值高于所有默认 Renderer。`MapChunkRoot/GroundTilemap` 临时改为 `Default` 排序层、`SortingOrder=-10000`，保证地面位于现有角色、敌人和投射物下方；正式九层配置完成后再迁回 `MapBackground`。未修改地图生成逻辑。

- 2026-08-20 | 接入第一版开放无限网格地图：新增 `MapModel`、`MapGridConfig`/`MapGridCatalog`、`MapSystem`、`MapChunkView` 与 `MapChunkFactory`。`MapGrid.asset` 使用用户创建的 `MapChunkRoot.prefab` 和 `Tile_Ground_Default.asset`，默认区块 32×32、加载半径 2、卸载半径 3；运行时仅在 `GameLoopSystem` Active tick 中围绕玩家加载地面区块，挂到现有 `WorldRoot/MapBackgroundRoot`，远处区块回收到对象池。未接入阻挡、道具、事件、寻路和箭头。Focused review 通过；`git diff --check` 仅报告既有用户文件换行提示，`dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` 通过，0 errors，保留既有 QFramework/MCP 程序集警告；Unity Play Mode 待管理员手动验证。

- 2026-08-20 | 收紧地图临时策划的存档与系统边界：地图模型仅保存 Seed、生成版本、区块增量、阻挡网格和导航缓存；玩家位置归 Run/Player，道具归 Item，事件归 MapEvent，指引归 WorldGuide。地图只发布区块与世界坐标通知，不解释其他模块的业务状态。未修改代码、场景、Prefab、UI 或 Docs Viewer，未提交 GitHub。

- 2026-08-20 | 在临时地图策划中加入第一阶段开放平面原型：使用 `WorldRoot/MapRoot/Grid/GroundTilemap` 和单一地面 Tile 填充可配置矩形范围，不生成阻挡物、不接入 Seed/区块/事件/寻路/卸载，仅验证移动、摄像机、敌人和攻击表现。未修改代码、场景、Prefab、UI 或 Docs Viewer，未提交 GitHub。

- 2026-08-20 | 更新临时地图策划草案的视觉方案：暂定“每区块独立 Grid/Tilemap + 独立 Prefab 内容”的混合实现；地表、装饰和阻挡碰撞使用 Tilemap，事件、地标和长期物品使用 Prefab，区块表现整体对象池化。未修改代码、场景、Prefab、UI 或 Docs Viewer，未提交 GitHub。

- 2026-08-20 | 更新临时地图策划草案的敌人寻路方案：采用基于阻挡网格的共享流场供大量敌人采样，玩家跨网格/区块、阻挡变化或敌人脱困时重建；按敌人半径膨胀阻挡区域，并将局部避让与全局寻路分开。未修改代码、场景、Prefab、UI 或 Docs Viewer，未提交 GitHub。

- 2026-08-20 | 更新临时地图策划草案的阻挡物规则：新增可配置图形模板、共享区块边界入口、主通路保护、Flood Fill/A* 连通性校验、失败回滚与开放布局兜底；明确角色移动阻挡与投射物阻挡分离，并记录敌人必须配套绕行/寻路。未修改代码、场景、Prefab、UI 或 Docs Viewer，未提交 GitHub。

- 2026-08-20 | 更新临时地图策划草案：明确 `WorldSeed` 为仅供继续游戏重建一致地图的内部存档字段，不向玩家展示或开放输入；补充 Unity 内置 Grid/Tilemap 仅作为区块表现与碰撞工具、而非无限世界生成器的边界。未修改代码、场景、Prefab、UI 或 Docs Viewer，未提交 GitHub。

- 2026-08-20 | 新增未提交的临时地图系统策划草案 `Docs/Temporary/InfiniteMapSystemPlan.draft.zh-CN.md`：定义无限区块、确定性 Seed、数据与表现分离、卸载恢复、长期物品、地图事件、远距离指引、存档边界和 QFramework 归属，仅用于后续讨论；未修改任何地图代码、场景、Prefab 或 UI，未生成 Docs Viewer 数据。

- 2026-08-19 | 定位 Mac 蓝屏资源问题：`EXE/Mac.app` 的包结构完整，但其 `StreamingAssets/AssetBundles` 仅包含 `Windows` 平台资源，Mac 运行时无法加载 UI/配置。`Assets/Editor/ProjectBuild.cs` 新增 `BuildMac()`，统一构建流程按 `BuildTarget.StandaloneOSX` 生成 `OSX` AssetBundle 与 Mac 应用；Windows 入口保持原行为。`git diff --check` 与 `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` 通过，0 errors；需重新执行 Mac 构建后手动验证。

- 2026-08-19 | 修复 `UIGameHUDPanel` 与 `UIGameOverPanel` 的空格误触按钮问题：局内 HUD/结算面板显示时关闭 EventSystem 键盘导航与 Submit，并清除当前选中对象；面板关闭后恢复导航，鼠标点击不受影响，空格技能输入保留。`git diff --check` 与 `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` 通过，0 errors；Unity Play Mode 待管理员手动验证。

- 2026-08-19 | 扩展 `.github/workflows/unity-windows-build.yml`：保留 `main` Artifact 打包，新增 `v*` Tag 触发、Windows 构建 ZIP 和 GitHub Release 创建；工作流声明 `contents: write`，使用内置 `GITHUB_TOKEN`，不新增账号 Secret。同步更新 `Docs/Guides/GitHubActions.zh-CN.md` 与 Docs viewer。静态检查通过；未执行真实 Tag 打包，待 PR 合并后手动推送版本 Tag 验证。

- 2026-08-19 | 管理员补齐仓库 Secrets `UNITY_EMAIL` 与 `UNITY_PASSWORD` 后，PR #17 的两次 Unity compile validation 均通过，`UNITY_LICENSE`、Personal License 激活和 Unity 项目编译验证正常；Static review 同样通过，PR 状态为 `MERGEABLE`，等待管理员审核合并。

- 2026-08-19 | 通过本机代理成功更新仓库 Secret `UNITY_LICENSE`，PR #17 的两次 Unity compile validation 已重新运行；GameCI 能读取许可证内容，但因 `UNITY_EMAIL` 与 `UNITY_PASSWORD` 为空仍报 `No valid license activation strategy`。静态 review 通过，项目代码无需修改；待管理员补充两个账号 Secret 后重新运行检查。

- 2026-08-19 | 解决 PR #17 与 `origin/main` 的冲突：保留 main 的 UTF-8 上下文规范与已合并的 progression 内容，重新生成全部 Docs viewer 分片，恢复 CI/Windows 打包的授权配置记录。冲突仅涉及 `Assets/AGENTS.md` 与生成的 `Docs/docs-content/doc-008.js` 至 `doc-021.js`；用户暂存前的 Enemy、渲染设置和 ProjectSettings 修改已通过 stash 保留，未纳入本次合并。待完成静态检查、C# 编译、合并提交和推送。

- 2026-08-19 | 合并冲突修复提交 `f023a7b` 已推送到 `feat/progression-save-system`，PR #17 已更新。`git diff --check` 通过，`dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` 通过（0 errors，保留 2 组既有 MCPForUnity 程序集警告）。用户五个未提交 Enemy/渲染设置文件已恢复在工作区，未提交。

- 2026-08-19 | PR #17 冲突修复后的 Actions 已完成：两次 Static review 通过，两次 Unity compile validation 失败，PR API 状态为 `MERGEABLE`/`UNSTABLE`。失败仍是 GameCI Unity 授权策略，不是合并冲突或项目编译错误；待 GitHub Secrets 配置有效后重新运行即可。

- 2026-08-19 | 诊断最新授权失败：仓库 Secret 列表中存在 `UNITY_LICENSE`，但 GameCI 容器仍报 `No valid license activation strategy`，且 `UNITY_EMAIL`、`UNITY_PASSWORD`、`UNITY_SERIAL` 未配置。结论是 `UNITY_LICENSE` 非完整有效 `.ulf` 文件内容或已被截断；工作流本身已正确传递该 Secret，待管理员重新导出并填写完整授权文件后重跑 Actions。

- 2026-08-19 | 根据 GameCI v4 官方激活规则进一步确认：Personal 许可证必须同时配置完整 `UNITY_LICENSE`、`UNITY_EMAIL`、`UNITY_PASSWORD`；`UNITY_SERIAL` 仅用于 Plus/Pro 等专业许可证。此前只配置 `UNITY_LICENSE` 会触发 `No valid license activation strategy`。已修正 `Docs/Guides/GitHubActions.zh-CN.md`，待管理员补充账号 Secret 后重跑 PR #17。

- 2026-08-19 | CI/Windows 打包实现及授权兼容修复已包含在 PR #17：`.github/workflows/ci-review.yml`、`.github/workflows/unity-windows-build.yml`、`Assets/Editor/ProjectBuild.cs`、GitHub Actions 指南和 Docs viewer 均已同步。工作流支持完整 `UNITY_LICENSE` 或 `UNITY_EMAIL`/`UNITY_PASSWORD`/`UNITY_SERIAL`；最近一次 Unity job 失败原因为仓库 Secrets 为空或无效，静态检查通过。

- 2026-08-19 | 用户本地 Enemy 碰撞与渲染设置修改继续保留在 stash，未进入 PR #17 或本次冲突合并：`Assets/Art/Prefabs/EnemyRoot.prefab`、`Scripts/Architecture/Enemy/EnemyFactory.cs`、`Scripts/Architecture/Enemy/EnemySystem.cs`、`Assets/Settings/UniversalRP.asset`、`ProjectSettings/ProjectSettings.asset`。合并完成后需恢复 stash 并继续由用户验证。

- 2026-08-18 | Fixed legacy saved level-up candidate text: existing active-run snapshots had already persisted the prior literal `\\uXXXX` perk name/description, so correcting the ScriptableObject alone did not repair Continue Game. `LevelUpOption.FromSaveData()` now decodes legacy Unicode escape sequences while restoring candidate display text, preserving the saved candidate choice and runtime target rather than discarding the snapshot. Focused review confirmed the conversion runs only for strings containing `\\u`; scoped `git diff --check` and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` passed with 0 errors and the two existing MCPForUnity warning groups. No UI, prefab, scene, commit, push, or PR change was made.

- 2026-08-18 | Fixed garbled Character Perk text in `Resources/Configs/Progression/CharacterExclusivePerkCatalog.asset`. The three display-name/description fields had been stored as unquoted literal `\\uXXXX` sequences, unlike the project’s normal UTF-8 text assets, causing Unity to display the escape text rather than Chinese. Replaced them with direct UTF-8 Chinese: `绝境意志`, `战术翻滚`, and `环流蓄能`. Focused asset review confirmed all six fields render as Chinese and no `\\uXXXX` remains in this catalog; scoped `git diff --check` passed. No UI, prefab, scene, code, commit, push, or PR change was made.

- 2026-08-18 | Implemented progression task 7: active-run snapshots now preserve the run phase/random state/timer, player location-health-invulnerability-dodge, stat and character-perk levels plus timed perk effects, queued/current level-up choices, Weapon and Skill runtime IDs/levels/Attack modifiers, enemy locations-health-movement and attack cooldowns, experience drops, projectiles, ground flames, transient explosion visuals, barrage runtimes, and player attack cooldowns. `RunSaveSystem` saves every 30 active seconds and on pause/exit/application pause/quit, reconstructs active entities from snapshots, and retains the save after Continue until victory or defeat. `RunSystem` now clears explosive-area runtime state during new-run and release flows; restored health publishes `PlayerHealthRestoredEvent` so the existing health bar refreshes. Weapon/Skill runtime IDs are restored exactly to keep cached level-up options and cooldown mappings valid. No UI hierarchy, prefab, or scene was changed. Focused review found and fixed missing explosive cleanup plus runtime-ID drift. `git diff --check` for changed snapshot paths and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` passed with 0 errors; the two existing MCPForUnity assembly-version warning groups remain. Unity MCP/Play Mode verification is still required for Random.State serialization and live restore behavior. Known precision boundary: restored piercing projectiles do not retain their pre-save hit-target set, so they can hit a previously pierced enemy once again after restore. No commit, push, or PR was created.

- 2026-08-18 | Implemented progression task 6 without UI, prefab, or scene changes. `CharacterExclusivePerkCatalog` now configures independent, three-level role perks; survivor receives `绝境意志` (≤50% health: all damage +15/30/45%), `战术翻滚` (after Dodge 1 ends: weapon projectile count +1/2/3 for 2s), and `环流蓄能` (after Skill 1: weapon cooldown -12/18/24% for 3/4/5s). `LevelUpSystem` supports weighted CharacterPerk candidates from the first level-up at 2x normal weight, alongside the existing exclusive-Skill slot. Timed effects use GameLoop Active time and reset at run start/release; levels remain intentionally outside snapshots until task 7. Added typed SkillUsed/perk events and applied the effects only in the central damage, weapon-cooldown, and projectile-count paths. Updated the uncommitted temporary progression draft. Focused lifecycle/QFramework/null/UI-boundary review and scoped `git diff --check` passed; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` passed with 0 errors and the two existing MCPForUnity assembly-version warning groups. No commit, push, or PR was created.

- 2026-08-18 | Unity refreshed Assembly-CSharp.csproj and now includes WorldRootLocator.cs. Dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false passes with 0 errors; the two existing MCPForUnity assembly-version warning groups remain. Codex Unity MCP handshake still returns HTTP 502, so Console and Play Mode verification remain unavailable.


- 2026-08-18 | Validation after WorldRoot migration: old container names have no script references and scoped diff check passed. Dotnet build cannot yet validate the migration because the generated Assembly-CSharp.csproj has not been refreshed to include WorldRootLocator.cs, producing unresolved WorldRootSlot errors. Refresh or regenerate project files in Unity before the next build; existing MCP assembly-version warnings remain separate.


- 2026-08-18 | Replaced legacy scene container dependencies with MainScene WorldRoot children. Added WorldRootLocator and routed PlayerRoot, EnemyRoot pool, experience drops, projectiles, explosion effects, and ground flames to PlayerRoot, EnemyRoot, PickupRoot, ProjectileRoot, CombatEffectRoot, and GroundEffectRoot. Pools resolve the scene root on spawn/release and do not recreate legacy containers. Scene hierarchy and Sorting Layer values remain user-owned. Focused reference review passed; Unity project-file refresh is still required before compilation because Assembly-CSharp.csproj is stale.


- 2026-08-18 | Rollback validation: no RenderLayerUtility, RenderLayerBinding, WorldRenderLayer, or custom Sorting Layer reference remains in gameplay source or TagManager. Dotnet compilation is temporarily blocked only because Unity has not regenerated Assembly-CSharp.csproj and it still lists the two deleted Presentation scripts; open Unity and refresh/regenerate project files before the next compile. MCP remains HTTP 502.


- 2026-08-18 | Reverted the uncommitted nine-layer render-layer implementation at user request. Removed RenderLayerUtility / RenderLayerBinding, their runtime assignments, and the nine Sorting Layers; FireBall, progression, and all user-owned UI/prefab edits remain. Render ordering is now manually owned by the user. Focused reference review found no remaining gameplay RenderLayer calls. Unity MCP remains unavailable (HTTP 502), so no Play Mode verification was possible.

- 2026-08-18 | Implemented the nine-layer world rendering contract. Added Sorting Layers in `ProjectSettings/TagManager.asset`, plus `RenderLayerUtility` / `RenderLayerBinding` under `Scripts/Game/Presentation/`. Player content, health-bar Canvas, enemies, experience drops, projectiles, explosions and ground flames are now assigned by runtime role during creation and pool reuse; explosive flame is `GroundEffect`, so it no longer renders over Enemy/Player, while explosions use `CombatEffect`. MainScene was inspected and contains no static Renderer/TilemapRenderer to migrate. Updated render-layer, script-reference, and uncommitted temporary-progression docs; the Docs viewer generator now excludes `Docs/Temporary` and regenerated 23 formal documents. Focused review passed; `Assembly-CSharp.csproj` passed with 0 errors but had not refreshed to include the new Presentation scripts, so those were separately compiled against the Unity 2022.3 assemblies with 0 errors. `git diff --check` found one pre-existing trailing whitespace issue in `Scenes/TestUIPanels/TestUILevelUpPanel.unity`, outside this task. Unity MCP remains HTTP 502, so Unity Console/Play Mode validation is pending. No commit, push, or PR was created.

- 2026-08-18 | Fixed combination-source Weapon reacquisition without UI, prefab, or scene changes. `PlayerLoadoutModel` now records Weapon IDs retired by a combination; `PlayerLoadoutSystem` excludes them from in-run acquisition, retires every consumed/replaced source, and rebuilds the state from equipped combination targets after `RunSaveSystem` restores a snapshot. FireBall ID 3 therefore no longer returns to the candidate pool after it evolves into Inferno FireBall ID 4, including after Continue Game. Added the deferred effect-rendering-layer system as task 8 in the uncommitted progression draft; `FireGroundFlame` still has no unified sorting rule and remains pending that task. Updated the Weapon and script-reference guides. Focused review and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` passed with 0 errors; the two existing MCPForUnity assembly-version warning groups remain. No commit, push, or PR was created.

- 2026-08-18 | Implemented progression task 5 without UI or prefab changes. `CharacterExclusiveSkillUpgradeCatalog` defines each character's initial Weapon/Dodge prerequisites, accepted final-form IDs, and one special Skill upgrade; `CharacterExclusiveSkillUpgradeSystem` validates those prerequisites and `LevelUpSystem` reserves candidate slot 1 until selection. Skill ID 1 now upgrades once from Level 1 to 2, is restored through the existing Skill snapshot, and makes `barrage-projectile` use the configured `终末环流` runtime parameters: 5 seconds, 0.0125-second emission, 1080 degrees/second, radius 2, damage x2, speed x1.5, and pierce +2. Added loadout Skill-level APIs and typed `SkillUpgradedEvent`; Attack execution now resolves a SkillRuntime separately from WeaponRuntime. Updated level-up/barrage/script docs, the temporary progression plan, and the generated Docs viewer. Focused review and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` passed with 0 errors; the two existing MCPForUnity assembly-version warning groups remain. Unity MCP still returns HTTP 502, so Console and Play Mode validation are pending. No commit, push, or PR was created.

- 2026-08-18 | Changed natural health regeneration to one-second gameplay ticks in `Scripts/Architecture/Progression/PlayerRegenerationSystem.cs`. The System now accumulates only GameLoop Active time, applies the full configured per-second amount once each second, and resets its remainder at run start/end. This prevents sub-pixel per-frame health-bar updates while preserving pause, level-up, death, and maximum-health handling through existing GameLoop and PlayerSystem guards. No UI, prefab, scene, or configuration changed. Focused review and compilation validation are pending.

- 2026-08-18 | Verified one-second regeneration ticks: the accumulator is zeroed per run and advances only from `GameLoopSystem` Active frame time; `PlayerSystem.RestoreHealth()` still limits every tick to missing health and keeps natural regeneration independent of recovery efficiency. Scoped `git diff --check` passed and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` completed with 0 errors. The two existing MCPForUnity assembly-version warning groups remain.

- 2026-08-18 | Fixed natural-regeneration lifecycle and pause catch-up: `PlayerRegenerationSystem` now stays registered when a living player owns regeneration, letting `GameLoopSystem` alone suspend it outside Active; `GameStart` feeds scaled `Time.deltaTime` into the run clock, and `RunTimerSystem` caps a gameplay frame at 0.05 seconds to prevent focus/pause recovery from restoring a long wall-clock interval at once. No UI, prefab, scene, or configuration changed. Focused review and compilation validation are pending.

- 2026-08-18 | Verified the natural-regeneration fix: focused review confirmed the regeneration System is registered only for a live owner with the upgrade, while `GameLoopSystem` still prevents all ticks outside Active; the capped clock protects every frame-driven gameplay System from pause/focus catch-up. Scoped `git diff --check` passed and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` completed with 0 errors. The two existing MCPForUnity assembly-version warning groups remain.

- 2026-08-18 | Fixed CS1929 in `Scripts/Architecture/Combat/ExplosiveAreaSystem.cs`: range-target reads now use `GameArchitecture.Interface.SendQuery`, because `AbstractSystem` is not an `ICanSendQuery` receiver. No UI, prefab, scene, or configuration changed. Focused review and Unity compilation validation are pending.

- 2026-08-18 | Verified the ExplosiveAreaSystem query fix: no remaining direct `this.SendQuery` use exists in that System; scoped `git diff --check` passed and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` completed with 0 errors. The existing two MCPForUnity assembly-version warning groups remain. Unity MCP connection attempt to `ProjectSurvivor@662e2df93b4f2899` returned HTTP 502, so Console and Play Mode validation remain unavailable.

- 2026-08-18 | Implemented progression task 2: `Resources/Configs/Progression/StatUpgradeCatalog.asset` defines seven equal-weight, five-rank percentage upgrades (damage +10%, cooldown -8%, absorb radius +20%, experience +15%, movement +8%, natural regeneration 0.5% max health/s, recovery efficiency +20%). `PlayerStatUpgradeSystem` owns levels and recomputes runtime modifiers; LevelUp now emits these as normal `Stat` candidates. Player attack executors and Dodge consume global damage/cooldown modifiers, Experience now uses float storage/calculation and one-decimal display helpers, and `PlayerRegenerationSystem` uses GameLoop ticks plus the generic `PlayerSystem.RestoreHealth()` API. Recovery efficiency is reserved for future external healing only. New stat levels are intentionally not restored by snapshots until the planned save-extension task. No UI hierarchy, prefab, or scene was modified; the temporary progression draft was updated and remains uncommitted. Focused review, Unity asset import inspection, source diff check, and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors and the 2 existing MCP assembly-version warning groups. No commit, push, or PR was created.

- 2026-08-18 | Unified level-up candidate handling around `LevelUpOption` and a cached `LevelUpModel.CurrentOptions` list. The current implementation generates up to three unique Weapon/Dodge options once per pending level, preserves them through UI refresh, and regenerates them if the selected runtime action fails. The option contract reserves Skill, Stat, WeaponCombination, and CharacterPerk types without implementing their gameplay/configuration yet. Changed `Scripts/Architecture/LevelUp/`, `Scripts/Architecture/Commands/GameplayCommands.cs`, and the existing LevelUp UI script only; no prefab, scene, or Bind hierarchy was modified. The temporary progression draft in `Docs/Temporary/` was updated and remains uncommitted by design. Validation pending.

- 2026-08-18 | Reviewed the level-up candidate change for stale command/query references, QFramework command/system/model boundaries, selection lifecycle, null-safe config lookup, and UI hierarchy impact; removed obsolete PlayerLoadout candidate helpers. `git diff --check` passes for agent-changed source/docs; the global check still reports only pre-existing trailing whitespace in the user-modified `Art/UIPrefab/UILevelUpPanel.prefab` and `Scenes/TestUIPanels/TestUILevelUpPanel.unity`, which were not edited. `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passes with 0 errors and 2 existing MCP assembly-version warning groups. Unity MCP returned HTTP 502, so Console/Play Mode verification remains pending. No commit, push, or PR was created.

- 2026-08-14 | Rebuilt the shared Git history to replace the prior checked-in Unity MCP source package with immutable upstream Git dependency `CoplayDev/unity-mcp@93f6d3d` (`MCPForUnity`, version `10.0.1-beta.1`). `Packages/manifest.json` and `Packages/packages-lock.json` now use the Git source; `Packages/unity-mcp-beta/` is no longer present in the project. The former local source was moved to `D:\Lihaofu\Game\UnityMcpLocalBackup-20260814` as a recoverable backup. JSON and Git-reference checks passed. Remote `main` was force-with-lease rewritten after PR #13 merged, preserving the rebased GameLoop content while removing the local package commit; merged remote branches `feat/add-unity-mcp-package` and `feat/unified-game-loop` plus all local backup/feature branches were deleted, leaving only `main`. The ProjectSurvivor Unity editor resolved the Git dependency into `Library/PackageCache/com.coplaydev.unity-mcp@93f6d3d311`, regenerated the project files, and normalized the lock entry; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` now passes with 0 errors and the two existing MCP assembly-version warning groups. GitHub may retain now-unreachable PR objects temporarily until server-side garbage collection.
- 2026-08-13 | 按用户要求卸载项目本地 project-validation 工作流，删除 .agents/codex-dynamic-workflows/project-validation/ 及其 YAML、提示词和 Schema；全局 codex-dynamic-workflow 技能保留。
- 2026-08-13 | 修复项目验证工作流的 Windows 命令定位：三个 rg 扫描节点改用 Codex 内置 rg.exe 绝对路径，解决 detached runner 报 [WinError 2] 而无法执行 QFramework、对象池生命周期和 UI 边界扫描的问题。仅修改 .agents/codex-dynamic-workflows/project-validation/workflow.yaml；待重新运行工作流验证。
- 2026-08-13 | 重构 `.agents/codex-dynamic-workflows/project-validation/workflow.yaml`：移除 Windows 下无法启动的后台 `agent` 节点与结构化汇总节点，改为纯 `command` 节点并行执行 `git diff --check`、`dotnet build`、QFramework API 扫描、对象池生命周期扫描和 UI 边界扫描；通过 `all_terminal` barrier 汇总终态。旧运行 ID `project-validation-20260813T023919Z-841a688e` 与 `project-validation-20260813T024559Z-b519e769` 作废，需用新配置重新启动。
- 2026-08-13 | Installed the global Codex skill `codex-dynamic-workflow` from `https://github.com/uiuedsdh/codex-dynamic-workflow` into `C:\Users\Administrator\.codex\skills\codex-dynamic-workflow`. The skill is valid according to `quick_validate.py`; no repository source, asset, scene, package, or project-local workflow definition was changed.
- 2026-08-12 | Resolved PR #10 merge conflicts against `origin/main`: retained `main` durable context/history and the main-menu return target in `UIGameOverPanel`; the resolution does not alter any UI hierarchy. Focused review and compilation validation pending.
- 2026-08-12 | Committed and pushed `3abc647` (`feat(ui): add main menu flow`) on `feat/unified-game-clock`. PR #9 had already merged before this push, so the main-menu commit requires a new pull request. User-local `Packages/manifest.json`, `Packages/packages-lock.json`, and `Packages/unity-mcp-beta/` remain excluded.
- 2026-08-12 | Integrated the user-created `UIMainMenuPanel` without changing its hierarchy or generated Bind file. Startup now opens the main menu; Start enters character selection, Settings is intentionally inert, and Quit calls `Application.Quit`. Character-selection Back, in-run Back, and game-over Return now converge on the main menu; the latter two retain the existing settlement and runtime-release command before navigation. Main-menu buttons are resolved from the user-owned `BG/Button_StartGame` and `BG/Button_QuitGame` hierarchy. Affected: `Scripts/Game/GameStart.cs`, `Scripts/UI/UIMainMenuPanel.cs`, `UICharacterSelectPanel.cs`, `UIGameHUDPanel.cs`, `UIGameOverPanel.cs`, and user-created main-menu assets. Focused review corrected the initial invalid direct button paths and a QFramework base-method name collision. `git diff --check` passed; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors and 2 existing MCPForUnity assembly-version warning groups.
- 2026-08-12 | PR #9 分支同步 origin/main 时解决 AGENTS.md 冲突：保留 main 的完整项目上下文和历史记录，并追加本分支的失败结算、重开修复、独立 RunSettlementModel/RunSettlementSystem 与 PR #9 记录。待完成合并提交与推送。
- 2026-08-12 | 提交 88c3017（feat(run): add defeat settlement flow）并推送 feat/unified-game-clock，创建 PR #9：https://github.com/wodemay/HaofuSurvivor/pull/9。包含失败结算、重开 PlayerRoot 注册修复、独立结算模块、UIGameOverPanel 接入与同步文档；本地 Packages MCP 改动未提交。
- 2026-08-12 | 失败或胜利先由 RunSettlementSystem 生成结算快照，再由重开/返回流程释放局内运行时对象。快照保存角色 ID、结果、存活秒数、局内等级、局内经验，并预留金币和局外经验字段（当前为 0）。RunSettlementModel 与 RunSettlementSystem 为独立脚本；RunSystem 仅编排时机与运行时释放。dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers 通过（0 errors，2 条既有 MCPForUnity 程序集版本警告）。
- 2026-08-12 | 重开流程固定读取 CharacterSelectionModel.SelectedCharacterId 并重新创建 PlayerRoot。PlayerModel 记录当前运行时 PlayerRoot 身份，旧对象销毁时不能注销新玩家；角色内容预制体不再承担玩家注册。

- 2026-08-11 | Pushed merge commit `442fe9f` to `feat/unified-game-clock`; PR #8 remains open against `main` and now points to this commit: https://github.com/wodemay/HaofuSurvivor/pull/8. The merge conflict shown by GitHub has been resolved. MCP package files under `Packages/` remain local and uncommitted.

- 2026-08-11 | Pushed `9ce1136` (`feat(weapon): add projectile upgrade path`) to `feat/unified-game-clock`, excluding user-local MCP package changes under `Packages/`. The previous PR #7 was no longer open, so created PR #8 into `main`: https://github.com/wodemay/HaofuSurvivor/pull/8. It contains the projectile weapon upgrade/evolution loop, reorganized Weapon configuration assets, Chinese display texts, and synchronized guides; it has not been merged.

- 2026-08-11 | Updated `Docs/Guides/WeaponUpgrade.zh-CN.md` and `Docs/Scripts/ScriptReference.zh-CN.md` for the relocated Weapon configuration paths, runtime modifier flow, projectile multi-shot/pierce behavior, evolution candidates, and the distinction between Chinese display text and non-localized internal identifiers. Documentation-only change; `git diff --check` passed and no obsolete English display text or old projectile-Weapon path remains in current Docs/configuration assets.

- 2026-08-11 | Localized all current configuration fields used as in-game display text: `ExperienceDrop_Standard.DisplayName`, both projectile Weapon names/descriptions, and every `Weapon_Projectile.LevelUpgrades.Description` are now Chinese. IDs, executor keys, asset filenames, internal event identifiers, and character text already stored as Chinese were unchanged. Focused field review confirmed all current display fields are Chinese; `git diff --check` passed.

- 2026-08-11 | Reorganized weapon configuration assets without changing GUIDs: global `WeaponCatalog` and `WeaponEvolutionCatalog` now live in `Resources/Configs/Weapon/`; all current projectile Weapon, evolution, Attack, and projectile-parameter assets now live in `Resources/Configs/Weapon/Projectile/`. Updated both Resource loader paths. `Combat/AttackCatalog` remains the central attack-ID index and continues to reference the relocated assets by GUID. No UI, prefab, or scene structure changed. Focused review, `git diff --check`, and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` completed with 0 errors; the 2 existing MCPForUnity assembly-version warnings remain.

- 2026-08-11 | Implemented the first complete runtime weapon-upgrade path for `Weapon_Projectile`: generic per-Attack modifier data is stored only in `WeaponRuntimeData`; AttackSystem applies cooldown modifiers and ProjectileAttackExecutor applies projectile count, damage, speed, and pierce modifiers. Added projectile multi-shot spread and per-launch hit tracking for penetration. Added Weapon 1 levels 2-5, evolved Weapon 2 / Attack 1003, and a level-5 WeaponEvolutionCatalog entry; LevelUp now presents evolution candidates and preserves the existing UI hierarchy. Added `Docs/Guides/WeaponUpgrade.zh-CN.md`. Focused review and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` completed with 0 errors; the 2 existing MCPForUnity assembly-version warnings remain.

- 2026-08-11 | Merged `origin/main` into `feat/unified-game-clock` to update PR #8 after PR #7 was merged. Resolved shared progression/level-up/configuration conflicts by retaining the complete weapon-upgrade implementation and the already-merged progression foundation. Focused review found no QFramework boundary, lifecycle/pooling, null-reference, or UI-structure issue; `git diff --check` and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors and 2 existing MCPForUnity assembly-version warnings. User-local MCP package changes under `Packages/` remain excluded.
- 2026-08-10 | Pushed `f3c0040` (`feat(progress): add experience and level-up loop`) to `feat/unified-game-clock`; PR #7 is now merged into `main`. It includes the experience-drop loop, standalone LevelUp module, the user-created level-up UI assets, configurations, and Chinese guides.
- 2026-08-10 | Prepared the current experience-drop and standalone level-up implementation for GitHub review on `feat/unified-game-clock`. The submission includes the user-created level-up UI assets, Experience/LevelUp scripts and configuration, supporting player/enemy/weapon/run updates, and synchronized Chinese documentation; it excludes user-local MCP package files under `Packages/`. `git diff --check` found no whitespace errors and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` completed with 0 errors. Existing QFramework and MCPForUnity warnings remain.
- 2026-08-10 | Separated level-up selection from experience processing. Added `LevelUpModel` and `LevelUpSystem`; Experience now only emits `PlayerLevelUpEvent`, while LevelUp owns queued selections, weapon-option queries, confirmation, and RunSystem pause/resume integration. Updated the existing UI script to query LevelUp state without modifying its hierarchy or Bind fields. Added `Docs/Guides/LevelUp.zh-CN.md` and synchronized the experience and script-reference guides. Focused review and `git diff --check` found no issue in the changed paths; Unity script refresh, `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers`, and a five-second MainScene Play Mode entry/exit completed with 0 errors. The 2 existing MCPForUnity assembly-version warnings remain.
- 2026-08-10 | Reworked experience-orb attraction into a persistent capture state. `ExperienceSystem` now captures a drop once within player absorb radius and never releases it for distance, then accelerates toward the player's current position using PlayerStatModel acceleration and maximum-speed attributes. Added the corresponding CharacterConfig fields and set all three current characters to acceleration 50 and maximum speed 30. Updated the Chinese experience guide and script reference. No UI, prefab, or scene hierarchy changed. Focused review and `git diff --check` found no issue in the changed paths; Unity script refresh, `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers`, and a five-second MainScene Play Mode entry/exit all completed with 0 errors. The 2 existing MCPForUnity assembly-version warnings remain.
- 2026-08-10 | Reconnected Unity MCP to `ProjectSurvivor@662e2df93b4f2899`. Confirmed `MainScene` is idle with no compilation or asset-refresh activity, and `read_console` returned 0 errors. No code, asset, scene, prefab, or configuration changed.
- 2026-08-10 | Implemented the experience and initial weapon-level-up loop. Added Experience Model/System/Factory, progression and standard-drop assets, pooled ExperienceOrb spawning from enemy death, player-radius absorption, queued level calculations, independent LevelUpSelection clock freeze, level-up UI item integration, and direct enemy/character/weapon configuration fields. All four current EnemyConfigs drop 1 experience; progression starts at 5 experience. No user-created UI, prefab, or scene hierarchy was modified. Focused review and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` completed with 0 errors and 2 existing MCPForUnity assembly-version warnings. Unity MCP asset refresh timed out and then reported no Unity session, so Play Mode and Console validation remain pending.
- 2026-08-10 | Fixed missing namespace closing braces in `Scripts/Architecture/Experience/ExperienceDropController.cs` and `ExperienceModel.cs`, resolving CS1513 errors. Focused review found no additional issue in those changed paths; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` completes with 0 errors and the existing 2 MCPForUnity assembly-version warnings.
- 2026-08-10 | Added a mandatory code-review gate: after every code change, review all changed paths for correctness, QFramework boundaries, lifecycle/pooling safety, null references, and unintended UI or prefab edits before validation or handoff.
- 2026-08-10 | Added a mandatory execution rule: every agent run and task action must read the complete `AGENTS.md` before proceeding, so the durable project context is always applied.
- 2026-08-10 | Updated root `Docs/Guides/ExperienceDrop.zh-CN.md` to reserve `EnemyConfig.DropTableId` with default value 0. The field is a future data boundary only: no drop-table configuration, catalog, probability rule, or runtime behavior is implemented; current drops still use the direct ExperienceDropConfig reference.
- 2026-08-10 | Revised root `Docs/Guides/ExperienceDrop.zh-CN.md`: experience absorption range is now a PlayerStatModel runtime attribute sourced from CharacterConfig, gem movement is distance-proportional without per-gem MagnetSpeed, and EnemyConfig directly references its ExperienceDropConfig instead of an ID lookup. No gameplay code, UI, prefab, scene, or configuration asset changed.
- 2026-08-10 | Moved `ExperienceDrop.zh-CN.md` to the project-root `Docs/Guides/` directory, removed the now-empty mistaken `Assets/Docs` directory and metadata, and established the durable rule that all future project documentation belongs under root `Docs/`, never `Assets/Docs/`. No gameplay code, UI, prefab, scene, or configuration asset changed.
- 2026-08-10 | Added root `Docs/Guides/ExperienceDrop.zh-CN.md`, defining the planned QFramework experience-drop loop, configuration contracts, pooled runtime ownership, enemy-death event contract, unified-clock pause behavior, and future upgrade-selection boundary. No code, UI, prefab, scene, or configuration asset was created.
- 2026-08-07 | Pushed non-functional approval-test commit `5045d90` to `feat/unified-game-clock`. PR #5 had already merged, so created PR #6 for the new branch commits: https://github.com/wodemay/HaofuSurvivor/pull/6. Repository rules require pull requests but set required approving reviews to 0.
- 2026-08-07 | Added a non-functional change-log entry solely to refresh PR #5 for repository approval-permission verification. No gameplay code, asset, scene, or configuration behavior changed.
- 2026-08-07 | Submitted the current Assets worktree to `feat/unified-game-clock` as `cc0cd2e` (`fix(runtime): stabilize pause simulation`), including the HUD integration, unified-clock pause safeguards, and fixed-step post-pause correction. Push succeeded through the configured HTTP/1.1 proxy. Existing PR #5 is open against `main` and contains this commit: https://github.com/wodemay/HaofuSurvivor/pull/5. MCP package files under `Packages/` remain intentionally uncommitted.
- 2026-08-07 | Validated the fixed-step pause correction with `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers`: 0 errors. The existing MCPForUnity-related `System.Net.Http` and `System.IO.Compression` assembly-version warnings remain.
- 2026-08-07 | Fixed the post-pause physics jump in `Scripts/Game/GameStart.cs`: `TickRunPhysicsCommand` now receives `Time.fixedDeltaTime`, not `Time.fixedUnscaledDeltaTime`. The unscaled value reports wall-clock elapsed time since the previous physics tick and included pause duration on resume; the fixed simulation step prevents Player, Enemy, and Projectile movement from catching up after a pause. Preserved and corrected the user-added physics diagnostic log. Source compilation is pending.
- 2026-08-07 | Restored the HUD pause button as an explicit toggle: when the run is active it dispatches `PauseRunCommand`; when paused it dispatches `ResumeRunCommand`. This supersedes a brief incorrect idempotent-pause interpretation. All gameplay paths remain gated by `RunTimerSystem.IsRunning()` and Unity `Time.timeScale`; source compilation is pending this correction.

- 2026-07-30 | Added this contributor guide and durable project-context policy. Verified the Assets layout, QFramework installation, gameplay/UI scripts, scenes, and empty Git history.
- 2026-07-30 | Added root `.gitignore` for Unity-generated files, IDE files, and the QFramework package cache. Git remote: `https://github.com/wodemay/HaofuSurvivor.git`; the initial project submission is on `main` and excludes local generated content.
- 2026-07-30 | Created initial project commit `ba5dc5e` (2,392 files). Integrated the remote repository's initial `LICENSE` and `README.md` with merge commit `c813a91`; no conflicts occurred. A push to `origin/main` failed because this environment could not connect to `github.com:443`; retry `git push -u origin main` after network access is restored.
- 2026-07-30 | Added UI workflow boundary: UI creation and structural editing are performed by the user; agents pause and wait for confirmation before UI-dependent implementation.
- 2026-07-30 | Added `Docs/GameArchitecture.md`: the implementation blueprint for the PC/mobile single-player survivor game, including QFramework boundaries, runtime/meta modules, persistent data ownership, UI boundary, and delivery order. No gameplay or UI assets were changed.
- 2026-07-30 | Added `Docs/GameArchitecture.zh-CN.md`, a Chinese document synchronized with the English architecture blueprint. No gameplay or UI assets were changed.
- 2026-07-30 | Added player-control foundation under `Scripts/Architecture/` and `Scripts/Game/PlayerController.cs`: Run, Input, Player, Stats, Damage, commands, and typed gameplay events follow QFramework boundaries. `GameStart` now initializes the architecture and starts a run. No UI or scene objects were created; `PlayerController` awaits attachment to a user-created player GameObject with `Rigidbody2D`.
- 2026-07-30 | Command-line validation was blocked by a stale generated `Assembly-CSharp.csproj` that does not yet list the new scripts; it reported missing new types from `GameStart`. Open or refresh the project in Unity before the next build to regenerate the solution and perform source compilation.
- 2026-07-30 | Fixed `GameStart` architecture command dispatch: `IArchitecture.SendCommand` requires a command instance, so run startup now passes `new StartRunCommand()`.
- 2026-07-30 | Unity regenerated `Assembly-CSharp.csproj`; `dotnet build Assembly-CSharp.csproj --no-restore` now succeeds with 0 errors. Two pre-existing assembly reference-version warnings from MCPForUnity/Unity remain.
- 2026-07-30 | Integrated user-created `UICharacterSelectPanel` without changing its hierarchy or Bind fields. Added dynamic `UICharacterSelectItem` behavior using `Transform.Find`, character catalog/selection/persistence modules, selection commands/events, and player initialization from the selected character definition.
- 2026-07-30 | Unity did not synchronize a new Character source folder into the generated project even after metadata was added. Character module types are therefore colocated in the already-imported `GameArchitecture.cs`; their QFramework responsibilities and public contracts are unchanged.
- 2026-07-30 | Verified character-selection integration with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; the two existing MCPForUnity/Unity assembly reference-version warnings remain.
- 2026-07-30 | Inspected the user-created character-selection prefab: its item template and `CharacterGroup_Content` bindings match the runtime item-generation contract, and its item children match the required `Transform.Find` names. Revalidated with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; the two existing MCPForUnity/Unity assembly reference-version warnings remain. Identified that the standalone UI test scene has no `GameStart` bootstrap.
- 2026-07-31 | Moved `UICharacterSelectItem` into its own `Scripts/UI/UICharacterSelectItem.cs` file as the item controller. The existing panel, prefab hierarchy, and QFramework Bind fields were not changed. Refreshed Unity's asset database so the generated project includes the new script; Unity Console has 0 errors and `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Replaced the hardcoded character list with `CharacterConfig` ScriptableObject data. `UICharacterSelectItem`, player registration, and `CharacterCatalog` now consume the same configuration type. Added `Character_Survivor`, `Character_Vanguard`, and `Character_Scout` under `Resources/Configs/Characters/`; their `Icon` fields intentionally remain empty until character art exists. Verified Unity loads all three assets, and `dotnet build Assembly-CSharp.csproj --no-restore` completes with 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Connected the startup selection flow without changing UI assets: `GameStart` opens the user-created `UICharacterSelectPanel` via QFramework UIKit, and confirmation now starts the run after saving the selected character ID. Unity Console has 0 errors and `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Fixed the character item initialization null reference: the two text nodes are nested under the template's `GameObject` child rather than the item root. Updated their `Transform.Find` paths. Verified in Unity Editor by cloning the actual item template and calling `UICharacterSelectItem.Initialize` with `Character_Survivor`, which completed and returned ID `survivor`; `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Added `CharacterConfig.PlayerPrefab` and QFramework `PlayerSpawnSystem`. Confirming selection now dispatches `StartSelectedCharacterRunCommand`, which creates the configured gameplay prefab only when it includes `PlayerController`, then starts the run. The three initial configuration assets have no player prefab reference because no player prefab exists yet; real spawn validation requires user-created role prefabs. Unity Console has 0 errors and `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | `Character_Survivor.PlayerPrefab` already referenced the user-created `Art/Prefabs/Square.prefab`; added the missing root `PlayerController` and Kinematic `Rigidbody2D` components so the temporary survivor can spawn. Unity Editor asset verification confirms both components are present, and `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Added `CharacterConfig.SkillId` as the reserved static identifier for each role's future skill group. Existing configuration assets intentionally keep it empty until skill-group IDs are designed. `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain); Unity MCP refresh was unavailable because its server returned HTTP 502.
- 2026-07-31 | Converted character and skill identifiers from strings to integers across configuration, catalog, selection model/storage/commands/events, UI item callbacks, and player model. `CharacterConfig` now exposes directly serialized `Id` and `SkillId` fields; initial role IDs are 1/2/3 and all current skill IDs are 0. Confirmed the three player-prefab references map to Square/Triangle/Circle. `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Removed the remaining `m` prefixes from `CharacterConfig` serialized fields and migrated the three existing asset YAML fields without changing their content references or numerical values. `dotnet build Assembly-CSharp.csproj --no-restore` has 0 errors (the two existing MCPForUnity/Unity reference-version warnings remain).
- 2026-07-31 | Added the QFramework run-timer foundation, timeline configuration, time-stage multiplier/event contracts, remaining-time HUD integration, and time-limit victory state. `RunTimeline.asset` currently defines a provisional 20-minute schedule with stages at 2:00, 5:00, 10:00, 15:00, and 18:20; enemy/spawn systems are not implemented yet and must consume these published contracts. `dotnet build Assembly-CSharp.csproj --no-restore` succeeds with 0 errors; 2 pre-existing MCPForUnity/Unity reference-version warnings remain. Unity Play Mode verification is pending because the Unity MCP server is returning HTTP 502.
- 2026-07-31 | Fixed `RunTimerSystem` null references on run start and frame advance by registering `RunTimerModel` in `GameArchitecture`. `RunTimerSystem` can now resolve its QFramework model through the architecture lifecycle.
- 2026-07-31 | Converted the run timer from countdown to elapsed-time mode. Removed match-duration clamping and automatic victory; `RunTimeline.asset` now acts solely as a configurable list of time-point events, with each `EventId` published through `RunTimelineStageReachedEvent`. The existing HUD time binding now displays elapsed `MM:SS` without changing user-created UI objects.
- 2026-07-31 | Added runtime player-camera binding. After `PlayerSpawnSystem` validates and instantiates the selected role, it adds or reuses `CameraFollow` on the `MainCamera`-tagged camera and binds the player transform. The follow behavior runs in `LateUpdate` and retains the existing camera Z position.
- 2026-07-31 | Verified camera-follow integration with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; 2 pre-existing MCPForUnity/Unity reference-version warnings remain. `CameraFollow` was colocated in `GameStart.cs` because the current generated project file did not include a newly created standalone script.
- 2026-07-31 | Staged the complete current worktree, including the user-created scenes/UI assets and the QFramework gameplay foundation, for the `main` branch repository update.
- 2026-07-31 | Created local commit `e5ec607` (`feat(core): add survivor gameplay foundation`). Push to `origin/main` is pending: this environment could not connect to `github.com:443` while running `git push -u origin main`.
- 2026-08-07 | Pause architecture decision: a future pause feature will use the run timer/run state as the single gameplay execution gate. Attacks, player movement, enemy movement/spawning, damage timers, and other in-run simulation must stop when the timer is paused; Unity `Time.timeScale` may only be an auxiliary engine-level freeze, never the business-state source of truth. No pause code or UI was added in this task.
- 2026-08-03 | Added QFramework enemy spawning and pursuit foundation. `TickEnemiesCommand` advances `EnemySystem` after the run timer; it creates temporary red-square runtime enemies beyond the active camera bounds, moves them toward the registered player, and scales interval/wave size over elapsed time plus timeline multipliers. No UI or scene hierarchy was changed. Validation is pending.
- 2026-08-03 | Verified enemy-system integration with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; 2 pre-existing MCPForUnity/Unity reference-version warnings remain. Unity Play Mode verification remains pending.
- 2026-08-03 | Refactored mixed architecture declarations into separate Character and Enemy scripts. GameArchitecture now only registers modules; EnemyConfig at Resources/Configs/Enemy.asset owns spawn and movement values, and EnemyFactory is the persistent singleton responsible for prefab or temporary-placeholder creation. Unity must refresh the project before compiling the new scripts; the current generated .csproj is stale.
- 2026-08-03 | Integrated the user-created Player root and HealthBarCanvas without changing either prefab hierarchy. Added PlayerRoot configuration, root-based character spawning, runtime Rigidbody2D/PlayerController setup, health-bar instantiation at HealthBarAnchor, and QFramework PlayerDamagedEvent-to-Slider binding. The new source was validated with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; the two existing MCPForUnity/Unity reference-version warnings remain. Unity Play Mode verification is pending.
- 2026-08-04 | Renamed `Art/Prefabs/Player.prefab` to `PlayerRoot.prefab` and renamed its root GameObject; its GUID was retained, so PlayerRoot configuration references continue to resolve. Added EnemyRoot configuration and factory integration: every configured enemy now spawns as EnemyRoot with its visual/content prefab parented to CharacterRoot. No user-created hierarchy was changed. `dotnet build Assembly-CSharp.csproj --no-restore` succeeds with 0 errors; the two existing MCPForUnity/Unity reference-version warnings remain.
- 2026-08-04 | Added `EnemyContainer` as a saved MainScene root and converted EnemyFactory from direct instantiate/destroy to per-EnemyConfig-ID FIFO pooling. Spawned EnemyRoot objects are parented under EnemyContainer; a run reset deactivates and returns them to their matching pool. Verified source compilation with 0 errors; Unity-generated project synchronization now includes the new root-config scripts. Unity Play Mode pooling verification remains pending.
- 2026-08-04 | Configured Git global `http.proxy` and `https.proxy` as `http://127.0.0.1:17891` for repository network operations; both values were read back successfully.
- 2026-08-04 | Prepared the current gameplay, scene, prefab, configuration, architecture, and durable-context changes for the next GitHub submission on `main`.
- 2026-08-04 | Created local commit `427bfc2` (`feat(gameplay): add rooted enemy pooling`) containing the current project state. Initial push through the configured proxy failed with a Git TLS unexpected-EOF connection error; remote submission is being retried.
- 2026-08-04 | Retried the GitHub submission with HTTP/1.1 through the configured proxy. Push succeeded: `origin/main` now contains commits through `083eb63`.
- 2026-08-04 | Added repository-local Codex skill `.codex/skills/project-survivor-qframework`. It captures the QFramework boundaries, user-owned UI workflow, root-prefab/configuration contracts, enemy pooling, validation, Git, and AGENTS handoff requirements for future sessions.
- 2026-08-04 | Prepared the validated repository-local ProjectSurvivor Codex skill and its durable-context update for local Git commit.
- 2026-08-04 | Local skill commit `3a70dd1` was not included in the preceding GitHub push; the branch is now being pushed so `.codex/skills/project-survivor-qframework` becomes available remotely.
- 2026-08-04 | GitHub push succeeded through `e6d0251`; the repository now contains `.codex/skills/project-survivor-qframework`.
- 2026-08-04 | Implemented the first configurable enemy attack path: all four current enemy configurations now reference contact attack ID 1001. Added attack catalog/configuration, QFramework attack system/commands, EnemyRoot trigger bridge and runtime Rigidbody2D setup; contact damage obeys its configured cooldown and the run damage multiplier, then uses the existing player-damage/health-bar flow. Source compilation succeeds with 0 errors; the two pre-existing MCPForUnity/Unity reference-version warnings remain. Unity Play Mode contact verification is pending.
- 2026-08-04 | Updated the repository-local ProjectSurvivor Codex skill with the configurable enemy attack contract and the current contact-attack runtime flow.
- 2026-08-04 | Prepared the configurable enemy contact-attack implementation, configuration assets, skill update, and durable-context changes for GitHub submission on `main`.
- 2026-08-04 | Added upgrade-ready player damage invulnerability. Default duration is 0 seconds; PlayerSystem now starts and ticks the duration only after future upgrades increase PlayerStatModel.DamageInvulnerabilityDuration. Verified with `dotnet build Assembly-CSharp.csproj --no-restore`: 0 errors; the two existing MCPForUnity/Unity reference-version warnings remain. Updated the project Skill with this contract.
- 2026-08-04 | Replaced the enemy-only attack module with the shared Combat architecture. Removed EnemyAttackConfig/Catalog/System, EnemyContactAttack, AttackType, and their legacy assets. Added generic attack configuration/catalog, runtime/system/commands, executor registry, CombatEntity factions, and ContactAttackTrigger; the existing enemy contact attack remains functional through ExecutorId `contact`. PlayerRoot and EnemyRoot now receive generic combat identities, so later player skills and enemy projectile/special behavior use the same contracts. Verified source compilation with 0 errors; 2 existing MCPForUnity/Unity reference-version warnings remain. Unity Play Mode verification is pending.
- 2026-08-04 | Prepared the shared Combat refactor, legacy enemy-attack cleanup, configuration migration, project Skill update, and durable-context changes for GitHub submission on `main`.
- 2026-08-04 | Created and pushed commit `a38c113` (`refactor(combat): unify attack execution`) to `origin/main`; the shared Combat architecture and removal of legacy enemy-only attack code are available on GitHub.
- 2026-08-05 | Added Chinese extension guides under `Docs/Guides/`: `NewAttack.zh-CN.md`, `NewEnemy.zh-CN.md`, and `NewCharacter.zh-CN.md`. They define the current Combat ExecutorId workflow and data/root contracts for safely adding attacks, enemies, and characters.
- 2026-08-05 | Added `Scripts/Editor/ConfigurationCreatorWindow.cs`, available at Unity menu `ProjectSurvivor/Configuration Creator`. It creates CharacterConfig or EnemyConfig with the next unused numeric ID, can create an empty content-prefab template, and automatically appends new enemies to EnemyCatalog. It never creates PlayerRoot, EnemyRoot, gameplay UI, or artwork. Updated extension and script-reference docs accordingly. `dotnet build Assembly-CSharp-Editor.csproj --no-restore` succeeds with 0 errors; 2 existing MCPForUnity/Unity reference-version warnings remain. Interactive Unity Editor verification is pending.
- 2026-08-05 | Updated the repository-local ProjectSurvivor Codex skill with the Configuration Creator contract and its explicit scope boundaries.
- 2026-08-05 | Expanded `Docs/Guides/NewAttack.zh-CN.md` into a step-by-step Chinese integration manual. It now specifies Unity folder/menu/Inspector operations, AttackCatalog registration, Enemy assignment, executor and trigger code locations, generic command calls, future executor-parameter configuration, and validation requirements.
- 2026-08-05 | Corrected the new-attack naming rule in `NewAttack.zh-CN.md`: use `Attack_<鎶€鑳藉悕绉?` because attack configurations are reusable and must not encode a particular Character, Enemy, or usage context.
- 2026-08-05 | Prepared the Configuration Creator editor, complete Chinese documentation set, ProjectSurvivor skill updates, and durable project context for GitHub submission on branch `feat/config-editor`.
- 2026-08-05 | Created and pushed `e8ea068` (`feat(editor): add configuration creator`) to `origin/feat/config-editor`. The remote branch now contains the Configuration Creator, all Docs additions, skill updates, and this project context; it is not merged into `main`.
- 2026-08-05 | Automated PR creation for `feat/config-editor` is blocked because GitHub CLI (`gh`) is not installed in this environment. The branch is fully pushed; create a PR manually from GitHub's branch comparison page when ready to merge it into `main`.
- 2026-08-05 | Located GitHub CLI at `C:\Program Files\GitHub CLI\gh.exe` (v2.92.0) and created PR #1 for `feat/config-editor` into `main`: `https://github.com/wodemay/HaofuSurvivor/pull/1`. `gh auth status` still reports its keyring token as invalid; re-authenticate with `gh auth login -h github.com` if subsequent GitHub CLI operations fail.
- 2026-08-04 | Added `Docs/Scripts/ScriptReference.zh-CN.md`: a Chinese, per-script reference for all current project scripts, grouped by module and documenting QFramework responsibilities, generated-code boundaries, and maintenance rules.
- 2026-08-04 | Created and pushed commit `8cb56e0` (`feat(enemy): add contact attack flow`) to `origin/main`; the contact-attack implementation and updated project Skill are available on GitHub.
- 2026-08-05 | Renamed the current shared collision attack executor ID from `contact` to `collision`. Renamed the bridge script and configuration asset to `CollisionAttackTrigger` and `Attack_Collision`; retained attack ID 1001 and its AttackCatalog reference. Documentation and the local project Skill were synchronized. Unity MCP refresh reported 0 Console errors; `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors and 2 pre-existing assembly-version warnings.
- 2026-08-05 | Removed `AttackConfig.TargetFaction` from the shared Combat contract and both current attack assets. `AttackSystem` now rejects same-faction targets and derives standard attack targeting from the runtime owner faction, so a single configuration can be used by Player or Enemy. Documentation and the local project Skill were synchronized. Unity MCP refresh reported 0 Console errors; `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors and 2 pre-existing assembly-version warnings.
- 2026-08-05 | Added generic projectile-attack foundation on branch `feat/projectile-attack`. `Attack_Projectile` (ID 1002) now references `ProjectileAttackParameter_Projectile` and the user-created Bullet prefab; added projectile executor, targeting query/system, trigger, pooled runtime factory, controller, and object-based damage command. Attack execution now retains concrete owner/target objects, while same-faction checks remain centralized in AttackSystem. Added `Docs/Guides/ProjectileAttack.zh-CN.md`. No EnemyConfig or CharacterConfig was assigned ID 1002, so the behavior is ready but not equipped until a user-selected Enemy attack list or the future SkillGroupId-to-AttackIds module assigns it. `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors and 2 pre-existing assembly-version warnings; Unity MCP Console has 0 errors. Enemy health/damage resolution remains a future module.
- 2026-08-05 | Renamed `CharacterConfig.SkillId` to `SkillGroupId` and migrated all three character assets without changing their value (`0`). The Configuration Creator label and Chinese documentation now use the same group-level name. Unity MCP Console has 0 errors; `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors and 2 pre-existing assembly-version warnings.
- 2026-08-05 | Added the player skill-group foundation. `SkillGroupConfig` now provides starting weapon, skill, and dodge IDs; `WeaponConfig` maps weapons to generic attacks. `PlayerLoadoutSystem` loads `CharacterConfig.SkillGroupId` after player creation, equips weapon attacks, stores skill/dodge IDs, and resets on player destruction. Added projectile starter group ID 1 and weapon ID 1, and assigned it to Character_Survivor; skills and dodge intentionally remain unimplemented data contracts. Added `Docs/Guides/SkillGroup.zh-CN.md`. Unity MCP Console has 0 errors; `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors and 2 pre-existing assembly-version warnings. A temporary hidden Editor object verified group 1 equips weapon 1 and adds `ProjectileAttackTrigger`; no scene changes were saved.
- 2026-08-05 | Added QFramework enemy-health resolution. `EnemyConfig.BaseHealth` was added and set to `20` for the four current enemy assets; `EnemyHealthSystem` registers spawned EnemyRoots, applies timeline health scaling, emits enemy damage/death events, and returns killed enemies through the existing EnemySystem/EnemyFactory pool flow. `DamageSystem` now routes Enemy targets to this system. With projectile attack ID 1002 dealing `10`, opening-stage enemies require two hits. Unity MCP Console reported 0 errors and `dotnet build Assembly-CSharp.csproj --no-restore` completed with 0 errors; the 2 existing MCPForUnity/Unity assembly-version warnings remain.
- 2026-08-05 | Created and pushed `feat/projectile-attack` commit `5d4709d` (`feat(combat): add projectile attack and loadout`). Opened PR #2 into `main`: `https://github.com/wodemay/HaofuSurvivor/pull/2`. The PR includes the shared projectile combat, player skill-group/loadout, collision naming, enemy-health/two-hit opening calibration, documentation, and durable-context updates. GitHub CLI required `HTTP_PROXY` and `HTTPS_PROXY` set to `http://127.0.0.1:17891` for API access.
- 2026-08-06 | Rebuilt `Docs/Scripts/ScriptReference.zh-CN.md` from the current `Assets/Scripts/` inventory. It now lists every script by module, corrects the shared combat/projectile/skill-group/enemy-health responsibilities, identifies generated Bind files and the `CameraFollow` colocated type, and records maintenance boundaries. No gameplay assets or code changed.
- 2026-08-06 | Added `Docs/Guides/AttackFlow.zh-CN.md`, documenting the current QFramework attack flow for player and enemy owners: configuration origin, runtime registration, targeting, cooldown, executor behavior, damage routing, death, and pool cleanup. It records that attacks are shared by owner faction and executor, not duplicated by actor type. No gameplay assets or code changed.
- 2026-08-06 | Updated the global Codex `add-feature` skill at `C:\Users\Administrator\.codex\skills\add-feature\SKILL.md`: sections 4 and 5 now use a Unity workflow (QFramework boundaries, assets/meta validation, Unity MCP refresh/Console checks, `dotnet build`, Unity Test Framework, and Play Mode lifecycle checks) instead of the former web/npm workflow. Validated with `python -X utf8 ...\quick_validate.py`; no project gameplay files changed.
- 2026-08-06 | Refactored player weapons into runtime Attack containers. WeaponConfig now supplies `InitialAttackIds`, `MaxLevel`, `CanUpgrade`, and per-level Attack add/remove data; PlayerLoadoutModel now owns distinct WeaponRuntimeData slots. PlayerLoadoutSystem supports equip, level upgrade, Attack-content replacement, direct Weapon replacement, configured evolution, cleanup, typed weapon events, commands, and a read-only query. Attack triggers now track a WeaponRuntime ID, allowing independent triggers for separate Weapon containers while EnemyConfig remains direct AttackIds. Added empty `WeaponEvolutionCatalog.asset`; real evolution targets must be user-configured as level-1 non-upgradeable WeaponConfigs. Unity MCP temporary unsaved-object validation passed initial equip, level upgrade, Attack replacement, direct replacement, and configured evolution to a level-1 non-upgradeable target. `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` has 0 errors with 2 pre-existing assembly-version warnings. Unity MCP's console response retains five historical edit-mode `Destroy` logs produced before AttackTrigger cleanup was corrected to use `DestroyImmediate`; the corrected validation rerun produced no new logs. Updated Weapon, skill-group, attack-flow, script-reference, and project-skill documentation.
- 2026-08-06 | Created and pushed commit `70ea2a5` (`refactor(weapon): model weapons as runtime attack containers`) to `feat/projectile-attack`; existing PR #2 is updated at `https://github.com/wodemay/HaofuSurvivor/pull/2`. The two MCPForUnity/Unity assembly-version warnings are accepted as existing environment warnings.
- 2026-08-06 | Recorded the Git/PR workflow: every push must be followed by an open-PR check for that branch to `main`; create and report a PR if absent, otherwise confirm the existing PR update. User review and merge remain GitHub-side actions.
- 2026-08-07 | Refined `Docs/Scripts/ScriptReference.zh-CN.md` for the runtime Weapon-container refactor: documented WeaponConfig level fields, stable runtime slots, evolution constraints, Attack Trigger ownership/lifecycle, and weapon Commands/Events. No gameplay assets or code changed.
- 2026-08-07 | Fixed pooled combat lifecycle defects: EnemyRoot attack triggers with runtime ID 0 are reused on activation instead of duplicating components; EnemyFactory and ProjectileFactory discard destroyed pooled references after scene changes; player spawn now aborts and unregisters when initial skill-group equipment fails; enemy ticking moved to FixedUpdate and uses Rigidbody2D.MovePosition. Unity Play Mode temporary validation confirmed a pooled enemy retains one collision trigger after reuse. Unity MCP Console has 0 errors and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` has 0 errors; 2 existing assembly-version warnings remain. Updated script reference, attack flow, and project Skill.
- 2026-08-07 | Added the unified run-time foundation on `feat/unified-game-clock`. `RunTimerSystem` is now the sole source of gameplay frame and physics deltas; player/enemy/projectile movement, projectile lifetime, attack cooldowns, and player invulnerability consume its output rather than independently reading Unity time. Added paused state, pause/resume commands/events, and `GetRunTimeStateQuery`; `Time.timeScale` is synchronized only by the run timer as an engine-level freeze. No UI was created or changed. `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` has 0 errors; the 2 MCPForUnity assembly-version warnings remain. Unity MCP refresh did not return before its timeout, so Unity Console and Play Mode verification remain pending.
- 2026-08-07 | Pushed `79f39ce` to `feat/unified-game-clock` and opened PR #4 into `main`: `https://github.com/wodemay/HaofuSurvivor/pull/4`. `Packages/manifest.json`, `Packages/packages-lock.json`, and the untracked `Packages/unity-mcp-beta/` directory were intentionally excluded because they are user-originated MCP package changes.
- 2026-08-07 | Added a non-functional unified-time maintenance note solely to validate the existing-PR push workflow. It does not change gameplay behavior, assets, or configuration.
- 2026-08-07 | Pushed `353959d` to `feat/unified-game-clock`. PR #4 was no longer open, so created replacement PR #5 into `main`: `https://github.com/wodemay/HaofuSurvivor/pull/5`. GitHub CLI GraphQL intermittently returned EOF; REST API verified the replacement PR is open and points to commit `353959d`.
- 2026-08-07 | Integrated the user-created UIGameHUDPanel controls without changing its hierarchy or Bind fields. Button_Pause now sends PauseRunCommand. Button_Back temporarily exits the active run through ExitRunToCharacterSelectionCommand, stops the timer, returns active enemies to the pool, destroys the current PlayerRoot, closes the HUD, and opens UICharacterSelectPanel; this is explicitly temporary until the final return flow is designed. dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers completed with 0 errors; existing framework/MCP warnings remain.
- 2026-08-07 | Fixed HUD pause and restart-run defects. Button_Pause now toggles PauseRunCommand and ResumeRunCommand from the unified run-time state. EnemySystem.Reset now ignores destroyed entries and invokes EnemyFactory.ReleaseAllActive as a factory-level fallback, preventing active EnemyRoots missed by the system list from surviving an exit/restart. dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers completed with 0 errors; 2 existing MCP assembly-version warnings remain. git diff --check reports only user-created UIGameHUDPanel prefab YAML trailing whitespace.
- 2026-08-07 | Verified Unity MCP HTTP transport is already running: `mcp-for-unity.exe` (Python PID 24836) connects to `http://127.0.0.1:8800` with `--project-scoped-tools`, and Unity owns the paired connection. The Codex-side `unityMCP` resource server still reports not ready, so MCP resource/tool validation cannot proceed until that registration becomes ready; do not start a duplicate transport process.
- 2026-08-07 | Hardened pause behavior so simulation cannot continue behind a frozen presentation. After the timer tick, GameStart skips gameplay and physics dispatch while paused; PlayerController, collision/projectile attack triggers, projectile hit handling, and DamageSystem each enforce the same run-time gate. Paused input is cleared to prevent stale movement on resume. `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` completed with 0 errors; 2 existing MCP assembly-version warnings remain. `git diff --check` reports only user-created UIGameHUDPanel prefab YAML trailing whitespace.
- 2026-08-13 | 新增局内进度恢复核心：Save/RunSaveData.cs、RunSaveStorage.cs、RunSaveSystem.cs 保存和恢复角色、时间、阶段、生命、位置、等级、经验及 WeaponRuntime；RunSystem.ContinueSavedRun() 重建 PlayerRoot 后恢复数据；GameStart 在应用暂停或退出时保存；新增 ContinueSavedRunCommand，并注册存档 Utility/System。敌人、经验球等瞬时对象不保存，恢复后按时间重新生成。主菜单继续按钮尚未接入，等待用户创建 UI 控件。dotnet build 通过（0 errors，2 条既有程序集版本警告）。
- 2026-08-13 | 接入用户生成的 UIMainMenuPanel Bind 字段 `Button_ContinueGame`：无存档时隐藏，有存档时显示；点击后发送 `ContinueSavedRunCommand` 恢复对局并关闭主菜单。修复查询调用为 `SendQuery(new HasSavedRunQuery())`。未修改 UI 层级或 Prefab 结构。dotnet build 通过（0 errors，2 条既有程序集版本警告）。
- 2026-08-13 | 新增长期协作规则：遇到 UI Bind、Prefab 层级、组件挂载或配置缺失导致无法接入时，必须先明确告知用户需要在 Unity 中补充的具体内容并暂停实施；不得默认假设或擅自修改用户拥有的 UI 结构。
- 2026-08-13 | 将局内存档策略调整为 30 秒自动保存；`RunSaveSystem.TickAutoSave()` 使用统一游戏时间累计间隔，暂停、返回主菜单、切后台和退出应用仍立即保存；胜利/失败结算会清除未完成对局存档，避免继续恢复已结束对局。修改 `RunSaveSystem.cs`、`RunSystem.cs`、`GameStart.cs`。代码审查通过；`dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` 通过（0 errors，2 条既有程序集版本警告）。
- 2026-08-13 | 重组并更新根目录 `Docs/` 全部项目文档：架构文档只保留 QFramework 边界与模块职责；`GameLogic.zh-CN.md` 只保留当前运行链路；新增 `Guides/RunSave.zh-CN.md` 说明局内快照；运行时间、经验、升级、技能组、Weapon、Attack、Projectile、Character、Enemy 指南均按单一主题重写；脚本索引改为职责清单；路线图只保留未实现事项。删除跨文档重复的完整流程和模块介绍；未修改代码、UI、Prefab、场景或配置。文档专属 `git diff --check -- Docs` 通过。

- 2026-08-13 | 继续收敛文档职责：GameLogic.zh-CN.md 不再重复存档字段与恢复边界，改为指向 Guides/RunSave.zh-CN.md；Guides/RunTime.zh-CN.md 移除存档时机小节，存档策略统一归 RunSave 指南。全文重复行检查仅保留中英文架构图这一对语言对应内容；git diff --check -- Docs 通过。
- 2026-08-13 | 新增纯本地文档查看器：Docs/index.html 提供目录、搜索和 Markdown 展示，Docs/docs-viewer.js 负责渲染，Tools/GenerateDocsViewer.ps1 自动读取 Docs/ 下全部 Markdown 并生成 Docs/docs-data.js。新增或修改 Markdown 后运行 powershell -ExecutionPolicy Bypass -File .\Tools\GenerateDocsViewer.ps1，再打开 Docs/index.html 查看；未修改 Unity UI、Prefab、场景或游戏代码。脚本审查通过，git diff --check -- Docs Tools 通过。
- 2026-08-13 | 优化文档查看器加载速度：Docs/docs-data.js 仅保存 17 个文档的标题、路径和分片文件名；Markdown 正文拆分到 Docs/docs-content/，docs-viewer.js 改为点击文档后懒加载并缓存，首屏不再载入全部正文。生成脚本同步更新分片。验证生成 17 个分片且 git diff --check -- Docs Tools 通过。
- 2026-08-13 | 实现普通闪避完整流程：新增 Architecture/Dodge/ 的 DodgeConfig、目录、运行时数据、模型和 DodgeSystem，新增闪避事件与请求/推进/升级 Commands；PlayerController 使用 Space 触发，GameStart.FixedUpdate 推进统一时间位移；闪避期间暂停普通移动并提供独立闪避无敌时间，DamageSystem 通过 PlayerSystem 统一拦截。StartingDodgeId 接入技能组，幸存者技能组配置为 Dodge 1；新增 DodgeCatalog.asset 与 Dodge_Basic.asset，并将闪避候选接入现有升级选择链路。存档增加 DodgeId/Level。focused review 修复了 StartRun 清理已装备闪避、升级参数未应用、Dodge 文件未纳入生成 csproj 和存档对象初始化问题；dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers 通过（0 errors，2 条既有程序集版本警告），git diff --check 通过。
- 2026-08-13 | 修复 Resources/Configs/Skills/DodgeCatalog.asset 缺失脚本：其 m_Script GUID 错误地指向资产 GUID，已改为 DodgeCatalogConfig.cs.meta 的脚本 GUID。dotnet build 通过（0 errors，2 条既有程序集版本警告），配置差异检查通过。
- 2026-08-13 | 修复闪避装备失败：DodgeCatalog.asset 对 Dodge_Basic.asset 的 GUID 原先为 31 位，Unity 无法解析目录引用，导致 DodgeCatalog.Get(1) 返回空并连带触发技能组装备失败。已将 Dodge_Basic.asset.meta 与目录引用统一为合法 32 位 GUID；目录脚本 GUID 保持指向 DodgeCatalogConfig.cs。dotnet build 通过（0 errors，2 条既有程序集版本警告），资源 YAML 检查通过。Unity 需刷新/重新导入该目录资产后验证。
- 2026-08-13 | Unity MCP 验证确认 Dodge_Basic.asset 原手工 YAML 不是 Unity 可识别资产：AssetDatabase.LoadAssetAtPath<DodgeConfig> 返回 null，MCP 显示 assetType Unknown、GUID 为空；DodgeCatalog.asset 本身可识别。已通过 Unity Editor 原生创建 DodgeConfig 资产，写入普通闪避参数，并重新绑定 DodgeCatalog 的 Dodge 1 引用。运行时 new DodgeCatalog().Get(1) 已验证返回 Dodge_Basic|1|HaoFuSurvivor.DodgeConfig；刷新 Unity 后资源链路正常。编译 0 errors，既有 2 组程序集警告。
- 2026-08-13 | 将闪避配置独立到 Assets/Resources/Configs/Dodge/：移动 DodgeCatalog.asset 与 Dodge_Basic.asset（GUID 保持不变），DodgeCatalog 的加载和回退扫描路径改为 Configs/Dodge；同步技能组与脚本索引文档。Unity MCP 刷新后确认两个资源分别识别为 DodgeCatalogConfig 与 DodgeConfig；dotnet build 通过（0 errors，2 条既有程序集版本警告）。
- 2026-08-13 | 新增 Docs/Guides/DodgeSystem.zh-CN.md，记录当前闪避配置、装备、输入 Command、统一时钟推进、位移、无敌、升级、存档和后续扩展边界；同步修正 Docs/Guides/SkillGroup.zh-CN.md 的闪避职责描述。仅修改文档，未改 UI、Prefab、场景或玩法代码；待重新生成文档查看器数据。

- 2026-08-13 | 完成模块化与故障隔离审计，新增 Docs/Guides/ModularityAudit.zh-CN.md。确认当前 QFramework 是职责模块化但仍共享 Assembly-CSharp，闪避配置错误会经 PlayerSpawnSystem -> PlayerLoadoutSystem -> StartSelectedCharacterRunCommand 阻断对局启动；同时记录 Player/LevelUp/Save/Enemy 配置等跨模块失败传播隐患。仅新增审计文档，未改代码、UI、Prefab、场景或配置；dotnet build 与 git diff --check 待本次文档变更后复核。

- 2026-08-13 | 按核心模块/可选能力策略完成第一阶段运行时隔离：PlayerLoadoutSystem.EquipInitialSkillGroup() 改为返回 SkillGroupEquipResult；SkillGroupConfig.RequireStartingWeapons 控制初始 Weapon 是否必需，Skill/Dodge 为可选；Dodge 装备失败会清空状态并降级禁用，不再阻止玩家生成；存档恢复跳过无效 Dodge/单个 Weapon，全部 Weapon 无法恢复时回滚到 RunPhase.None 并保留原存档；Dodge 配置运行中丢失会清理激活状态。同步更新 Docs/Guides/ModularityAudit.zh-CN.md。代码审查通过；文档查看器生成 19 个文档；git diff --check 通过；dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers 通过（0 errors，2 条既有程序集版本警告）。

- 2026-08-13 | 复核运行时隔离改造：技能组缺失现在视为核心 Weapon 装配失败并阻止启动；RequireStartingWeapons 默认 true；存档恢复统计有效 Weapon，全部无效时返回失败；同步 SkillGroup.zh-CN.md 与 ScriptReference.zh-CN.md，重新生成 19 个文档分片。代码审查确认无新增 QFramework 边界、空引用或 UI 结构问题；git diff --check 通过；dotnet build 0 errors，2 条既有程序集版本警告。

- 2026-08-13 | 提交并推送 7af22d2（refactor(runtime): isolate optional abilities）到 feat/unified-game-clock，排除本地 Packages/manifest.json、Packages/packages-lock.json 和 Packages/unity-mcp-beta/。已创建 PR #11：https://github.com/wodemay/HaofuSurvivor/pull/11，目标 main；等待用户审核和合并，未自行合并。

- 2026-08-14 | 处理 PR #11 与 origin/main 冲突：六个冲突文件（AGENTS、主菜单 Prefab/测试场景、GameStart、UIMainMenuPanel 及 Designer）保留当前分支的继续游戏、存档查询、Bind 和应用生命周期保存逻辑，未改 UI 层级；生成合并提交 656b1f2。dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers 通过（0 errors，2 条既有程序集版本警告）。推送因代理 TLS unexpected EOF 暂未成功，PR #11 仍指向旧提交 cb3528a，需网络恢复后执行 git push origin feat/unified-game-clock。

- 2026-08-14 | Local main fast-forwarded to origin/main at 52abff5; ahead/behind count is 0/0. User-local MCP package changes remain uncommitted in Packages/manifest.json, Packages/packages-lock.json, and Packages/unity-mcp-beta/. The pre-sync AGENTS snapshot remains in stash@{0}.

- 2026-08-14 | 统一整理 Docs Markdown 格式：为 Attack、Projectile、Character、Enemy、LevelUp、Weapon、SkillGroup、RunTime、RunSave、Experience 和 Dodge 指南补齐范围说明及稳定的二级章节；统一资源路径、验证章节和扩展边界，未改变玩法语义。重新运行 Tools/GenerateDocsViewer.ps1，生成 19 个文档分片；git diff --check -- Docs Tools 通过。

- 2026-08-14 | 补齐文档统一格式的第二轮：LevelUp、RunTime、SkillGroup、WeaponSystem、WeaponUpgrade 增加统一范围说明并明确相互引用；再次生成 19 个文档分片，git diff --check -- Docs Tools 通过。

- 2026-08-14 | Documentation synchronized with the unified GameLoop runtime: GameStart now sends only root frame/physics commands; RunTimerSystem remains the sole gameplay-time source; GameLoopSystem dispatches only registered IRunUpdateable/IRunFixedUpdateable systems; InputSystem reads left/right Shift for dodge; ProjectileAttackTrigger was removed in favor of automatic projectile executors and ProjectileSystem batch ticking. Rewrote all 19 Docs Markdown files, regenerated Docs viewer data, and verified git diff --check for Docs and Tools. No UI, prefab, scene, asset, code, commit, push, or PR change was made by the documentation pass.

- 2026-08-14 | Prepared feat/unified-game-loop for review: focused lifecycle and QFramework review found no stale gameplay Update/FixedUpdate or removed legacy Tick references; user prefab edits retained; docs viewer regenerated. git diff --check and dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers passed with 0 errors and 2 existing MCP assembly-version warning groups.

- 2026-08-14 | Pushed fba12b1 (refactor(runtime): centralize game loop) on feat/unified-game-loop and opened PR #13: https://github.com/wodemay/HaofuSurvivor/pull/13. The PR contains the unified tick refactor, user prefab updates, auxiliary code-reading guide, synchronized Docs/viewer data, and validation record; it has not been merged.
- 2026-08-14 | Repaired AGENTS.md mixed GBK/UTF-8 corruption, removed invalid control characters, and rewrote the file as UTF-8 without BOM. Updated the stale runtime, player-content, pooling, and projectile contracts to match the unified GameLoop implementation. Strict UTF-8 decoding and replacement/control-character scan passed; no gameplay source, UI, prefab, scene, or configuration changed.
- 2026-08-17 | Implemented survivor Skill ID 1, `环形弹幕`: `SkillCatalog`/`SkillRuntimeData` now equip skills independently from Weapon containers, using negative runtime IDs to keep Attack lifecycle cleanup isolated. Attack ID 1004 (`barrage-projectile`) fires three 8-projectile rings with 0.15-second intervals through `BarrageProjectileSystem`, `ProjectileFactory`, and the unified GameLoop; it runs without a target, freezes during pause/level-up, and is cleared on run start, exit, and restart. Added immutable skill/attack/parameter assets under `Resources/Configs/Skills/`, assigned the skill to survivor skill group 1, and updated survivor display text plus Chinese guides/docs viewer. Focused review and `git diff --check` passed; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors and the 2 existing MCP assembly-version warning groups. Unity MCP returned HTTP 502, so Unity Console/Play Mode verification remains pending.
- 2026-08-17 | Pushed `c443ad6` (`feat(skill): add survivor barrage`) to `feat/survivor-barrage-skill` and opened PR #14: https://github.com/wodemay/HaofuSurvivor/pull/14. GitHub CLI GraphQL creation could not connect to `api.github.com`; the authenticated GitHub REST endpoint created the PR successfully. Do not merge without user review.
- 2026-08-17 | Changed survivor Skill ID 1 from automatic to manual activation. Space now sends `RequestSkillCommand`, `PlayerLoadoutSystem` executes only equipped skill runtimes, and `AttackSystem` distinguishes target requirements through `IAttackExecutor`; the targetless `barrage-projectile` executor is manual-only and has a 15-second cooldown. Updated the barrage guide, script index, and viewer chunks `doc-016.js`/`doc-019.js`. No UI, prefab, or scene was modified. Focused review confirmed the QFramework command chain, cooldown tick, pause gate, skill runtime cleanup, and no pooled-object impact. `git diff --check` and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors; 2 existing MCP assembly-version warning groups remain. Unity MCP Play Mode validation is pending its recovery.
- 2026-08-17 | Pushed `43f59f8` (`fix(skill): trigger survivor barrage manually`) to `feat/survivor-barrage-skill`; it updates existing PR #14 for user review and does not merge it.
- 2026-08-17 | Added project-local Skill `.codex/skills/character-generation/` for natural-language playable-character packages. It directs future agents to allocate Character/SkillGroup/Skill/Dodge/Attack IDs automatically, create and catalog Config assets through Unity serialization, retain the survivor's starting Weapon and temporary content prefab, and wire the generated Skill/Dodge without Inspector binding. The Skill explicitly preserves UI/prefab ownership and identifies special-dodge behavior as a required code extension rather than a config-only claim. `quick_validate.py` passed; no gameplay code, UI, prefab, scene, or configuration asset was changed.
- 2026-08-17 | Reorganized scripts and runtime Configs by module without changing gameplay semantics. `Scripts/Game/` is now split into Bootstrap, Camera, Combat, and Player Unity bridges. Config assets now use `Character/`, `Enemy/`, `Player/`, `Combat/Attack|Weapon|Skill|Dodge/`, `Progression/Experience/`, and `Run/`; every affected `Resources.Load` path, Configuration Creator path, project Skill, guide, and docs-viewer entry was updated. Asset `.meta` files moved with their assets, preserving all GUID references. Added `Docs/Guides/DirectoryLayout.zh-CN.md`. Focused review found no stale old load paths, duplicate assets, or UI/prefab/scene edit. `git diff --check` and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors; 2 existing MCP assembly-version warning groups remain. Unity Editor import/Play Mode verification remains pending.
- 2026-08-17 | Reworked survivor Attack ID 1004 from three fixed eight-direction bursts into a three-second orbiting emission circle. `BarrageProjectileSystem` selects a random initial orbit angle, rotates the circle at 180 degrees per second, spawns immediately and then every 0.1 seconds from a random point within the circle, and fires opposite `InputModel.LastMovementDirection`. `BarrageProjectileAttackParameterConfig` now defines duration, projectile interval, orbit radius, emission radius, and orbit speed; current values are 3s, 0.1s, 1.25, 0.35, and 180. The 15-second skill cooldown, shared projectile pool, unified clock pause behavior, and existing UI/prefabs remain unchanged. Updated the barrage guide, script index, and docs viewer. Focused review, `git diff --check`, and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors; 2 existing MCP assembly-version warning groups remain. Unity Play Mode verification remains pending.
- 2026-08-17 | Adjusted survivor Attack ID 1004 to the requested yo-yo motion: its emission circle now advances clockwise and every projectile travels along the circle's instantaneous clockwise tangent. `BarrageProjectileSystem` no longer reads player movement direction. Updated `Docs/Guides/SurvivorBarrageSkill.zh-CN.md`; no UI, prefab, scene, or configuration asset changed. Focused review and validation are pending.
- 2026-08-17 | Verified the survivor yo-yo motion change: focused review confirmed the clockwise angular update, its matching tangent vector, unified `GameLoopSystem` scheduling, and unchanged projectile-pool lifecycle. Regenerated the Docs viewer (21 documents). `git diff --check` passed; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors and the 2 existing MCP assembly-version warning groups. Unity Play Mode verification remains pending.
- 2026-08-17 | Increased the survivor yo-yo barrage base density and speed through only `BarrageProjectileAttackParameter_SurvivorBarrage.asset`: interval is now 0.04 seconds (about 76 projectiles over 3 seconds including the immediate first shot) and move speed is 16. The change remains scoped to Attack ID 1004 and does not modify standard projectile Weapons, UI, prefabs, or scenes. Documentation and validation are pending.
- 2026-08-17 | Reviewed and validated the barrage parameter tuning: the shared projectile pool has no fixed runtime cap and each projectile remains registered and released through `ProjectileSystem`/`ProjectileFactory`; the 0.04-second cadence stays on the existing unified GameLoop. Regenerated the Docs viewer (21 documents). `git diff --check` passed; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors and the 2 existing MCP assembly-version warning groups. Unity Play Mode verification remains pending.
- 2026-08-17 | Corrected the survivor yo-yo barrage direction: the emission circle still orbits clockwise, but Attack ID 1004 projectiles now use the outward player-to-circle radius (the yo-yo string direction), rather than the orbital tangent. Updated the barrage guide; no UI, prefab, scene, or configuration asset changed. Focused review and validation are pending.
- 2026-08-17 | Verified the yo-yo string-direction correction: `orbitDirection` is both the center offset and the outgoing projectile vector, so all randomly positioned shots remain parallel to the player-to-circle string; no input, target, cooldown, pause, or pooling dependency changed. Regenerated the Docs viewer (21 documents). `git diff --check` passed; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors and the 2 existing MCP assembly-version warning groups. Unity Play Mode verification remains pending.
- 2026-08-17 | Tuned survivor Attack ID 1004 for a stronger ultimate: orbit speed increased from 180 to 720 degrees/second and projectile interval reduced from 0.04 to 0.025 seconds (about 121 shots over 3 seconds including the immediate first). `ProjectileFactory` remains the shared dynamically growing pool; no behavior, UI, prefab, scene, or unrelated Weapon configuration changed. Documentation and validation are pending.
- 2026-08-17 | Reviewed and validated the stronger barrage tuning: the 0.025-second interval remains clamped safely by the 0.01-second minimum and the existing `while` loop catches up without skipping shots; `ProjectileFactory` dynamically creates/reuses concurrent instances without a hard pool limit. Regenerated the Docs viewer (21 documents). `git diff --check` passed; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors and the 2 existing MCP assembly-version warning groups. Unity Play Mode verification remains pending.
- 2026-08-17 | Increased only the survivor barrage orbit rate from 360 to 720 degrees/second; its 0.025-second projectile cadence and move speed 16 are unchanged. Documentation and validation are pending.
- 2026-08-17 | Reviewed and validated the 720-degree orbit update: it uses the existing per-frame orbit calculation and retains the outward string-direction projectile vector, so attack ownership, cooldown, pause gating, and projectile lifecycle are unchanged. Regenerated the Docs viewer (21 documents). `git diff --check` passed; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors and the 2 existing MCP assembly-version warning groups. Unity Play Mode verification remains pending.
- 2026-08-17 | Extended active-run snapshots to preserve Skill runtime IDs and levels, then restore their Attack registration after loadout reset. `RunSaveSystem.Restore()` now rebinds PlayerLoadoutModel to the new PlayerRoot before rebuilding its loadout; legacy snapshots without the skill field fall back to the selected character's initial skills. Updated the run-save guide. Focused review and validation are pending.
- 2026-08-17 | Reviewed and validated the continue-run skill restoration fix: `HasSkillSnapshot` distinguishes new snapshots from legacy ones, restored skills are configured through `PlayerLoadoutSystem.EquipSkill()` and therefore use the same AttackSystem registration path as a fresh run, and re-binding the model owner restores `RequestSkillCommand -> TryUseSkills()` eligibility. Invalid individual skills only log and skip, preserving the optional-ability boundary. Regenerated the Docs viewer (21 documents). `git diff --check` passed; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` passed with 0 errors and the 2 existing MCP assembly-version warning groups. Unity Play Mode verification remains pending.
- 2026-08-17 | Committed the current module reorganization, character-generation Skill, survivor barrage implementation/tuning, and continue-run Skill restoration as `18aae18` (`feat(skill): refine survivor barrage and resume`) on `feat/survivor-barrage-skill`. Push to `origin` failed twice with `TLS connect error: unexpected eof while reading`, both directly and through `HTTP_PROXY`/`HTTPS_PROXY` with HTTP/1.1; PR #14 therefore has not yet received this commit. No Packages files were included.
- 2026-08-17 | Retried with the configured local proxy and HTTP/1.1; push succeeded. `origin/feat/survivor-barrage-skill` now includes `18aae18` and `46c38ea`, updating the existing PR #14. No merge was performed.
- 2026-08-17 | GitHub 实际接口确认此前不存在该分支的开放 PR；已通过 REST 创建 PR #15（`feat/survivor-barrage-skill` -> `main`）：https://github.com/wodemay/HaofuSurvivor/pull/15。PR 当前存在与 main 的合并冲突，待后续处理；未执行合并。
- 2026-08-17 | 将 `origin/main` 合入 `feat/survivor-barrage-skill` 以解决 PR #15 冲突。保留后续模块目录重组、Skill 快照恢复，以及 Attack 1004 的三秒顺时针轨道弹幕参数（720°/秒、0.025 秒间隔、速度 16）；未恢复 main 中已过时的三段固定八向弹幕实现。重建 Docs 查看器数据；未修改 UI、Prefab 或场景。无冲突标记，`git diff --check` 与 `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers` 均通过（0 errors，保留 2 组既有 MCP 警告）。合并提交 `e546fda` 已推送至 PR #15 分支。
- 2026-08-17 | 新增未提交的临时规划 `Docs/Temporary/RunProgressionExpansion.draft.zh-CN.md`：拆分局内成长扩展的候选契约、属性、Weapon 获取、组合、专属 Skill/Dodge、角色专属升级和存档任务，并列出待管理员确认的规则。该草案不生成 Docs 查看器数据、不提交或推送 GitHub；未修改玩法代码、UI、Prefab、场景或配置。
- 2026-08-17 | 更新未提交的局内成长临时草案：确认升级候选最多 3 个、允许不同 Weapon 同类候选同屏但禁止同一 Weapon 同一级重复、候选不足时仅显示可选项且必须选择、候选执行失败后重新生成；取消第一版刷新、跳过、锁定和稀有度。其余第一任务边界仍待管理员确认；未修改玩法代码、UI、Prefab、场景或配置。
- 2026-08-18 | Implemented progression task 3 without changing any user-owned UI, scene, or prefab hierarchy. `PlayerLoadoutModel` now enforces six Weapon slots; `PlayerLoadoutSystem` distinguishes in-run acquirable Weapons and rejects duplicate acquisition or evolution into an already-equipped Weapon. Stat upgrades likewise enforce six first-acquired attribute slots while allowing owned attributes to reach their own caps. `LevelUpSystem` now offers eligible new Weapons alongside upgrades/evolutions. Added in-run-acquirable FireBall Weapon ID 3 / Attack ID 1005 under `Resources/Configs/Combat/Weapon/FireBall/`, using the existing projectile executor and user-created `Art/Prefabs/Attack/FireBall.prefab`; its five-level path is damage, cooldown, extra projectile, then damage/pierce. Updated the uncommitted temporary progression draft only. Focused review covered command/system/model boundaries, null catalog handling, duplicate attack registration, slot transitions, and UI/prefab isolation. FireBall asset-reference checks and `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` passed with 0 errors; 2 existing MCP assembly-version warning groups remain. The default compiler invocation once terminated with `0xC0000005`; the no-shared-compilation retry succeeded. No commit, push, or PR was created.
- 2026-08-18 | Implemented progression task 4 and replaced the obsolete `WeaponEvolutionCatalog` path with recipe-driven `WeaponCombinationCatalog` / `WeaponCombinationSystem`. Recipes support one-or-more Weapon prerequisites, optional non-consumed stat requirements, 70% candidate rolls, single-slot replacement, and multi-Weapon removal/replacement. Added validation recipes Weapon 1 Lv5 + Stat 1 -> Weapon 2 and Weapon 3 Lv5 + Stat 2 -> Weapon 4. Weapon 4 (`炼狱火球`, Attack 1006) introduces executor `explosive-projectile`: a FireBall impact sends a Command to `ExplosiveAreaSystem` for radius-2.5 45-base-damage explosion plus a radius-1.9 ground fire for 3 seconds, ticking every 0.5 seconds for 10 base damage. Effects and projectiles pool safely, use GameLoop time, honor pause/end-run cleanup, and do not add any Update to Prefabs. User-created FireBall visual Prefabs were integrated without structural edits; per request, `Art/Prefabs/Attack/FireBall.prefab` moved to `Art/Prefabs/Attack/FireBall/FireBall.prefab` with its metadata, preserving GUID references. Updated Weapon/Attack/script guides and the uncommitted temporary progression draft. Focused review fixed flame catch-up ticking, chance clamping, destroyed pooled-reference handling, source-attack cleanup, duplicate target checks, and asset GUID/fileID integrity; scoped diff check passes. Unity MCP returned HTTP 502 and the generated `Assembly-CSharp.csproj` has not refreshed to include the new scripts, so Unity compilation/Play Mode validation remains pending. No commit, push, or PR was created.
- 2026-08-18 | Created and force-with-lease pushed `feat(progression): expand run progression systems` on `feat/progression-save-system`, then opened PR #16: https://github.com/wodemay/HaofuSurvivor/pull/16. The PR contains the in-run progression, FireBall evolution, WorldRoot migration, expanded active-run snapshot/restore, and Chinese text fixes. `Docs/Temporary/` remains intentionally uncommitted. Focused review passed; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` passed with 0 errors and the two existing MCPForUnity warning groups. `git diff --check` still reports only user-owned trailing whitespace in `Scenes/TestUIPanels/TestUILevelUpPanel.unity`.
- 2026-08-20 | 修复开局地图闪烁：`MapSystem.PrepareForRun()` 在 `RunSystem.StartRun()` 的 `GameLoopSystem.BeginRun()` 前同步生成玩家加载半径内的全部区块；正常运行中的新区块仍受 `MaxChunkOperationsPerTick` 限制，避免移动时产生单帧卡顿。变更路径为 `Scripts/Architecture/Map/MapSystem.cs` 与 `Scripts/Architecture/Run/RunSystem.cs`。已完成 focused review 与 `git diff --check`，待执行 Unity/程序集编译验证。
- 2026-08-20 | 已完成开局地图预热修复验证：`dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` 通过，0 errors，保留既有 System.Net.Http/System.IO.Compression 程序集冲突警告；`git diff --check` 通过。Unity Play Mode 仍需管理员手动确认开局不再闪烁。
- 2026-08-20 | 修正地面贴图显示比例：`Ground.png` 实际为 282×282 像素，原 `spritePixelsToUnits: 32` 使单个 Tile 覆盖约 8.8×8.8 世界单位；已将 `Ground.png.meta` 的 Pixels Per Unit 调整为 282，使每个地面 Tile 对应一个 1×1 网格单元。未修改 Tilemap 生成逻辑。
- 2026-08-20 | 提交前验证当前版本：程序集编译通过，0 errors，保留既有 MCP 程序集警告；Unity Windows 批处理打包因 ProjectSurvivor 已被另一个 Unity 实例打开而中止，未生成 Build 输出，未强制关闭用户编辑器。当前提交范围包含地图系统、地面资源、场景与现有运行时改动；`Docs/Temporary/` 与本地 `Mac/` 成品继续排除。
- 2026-08-20 | 创建提交 `e248d3f`（`feat(map): add infinite tilemap world`），包含当前地图与地面比例修复等已暂存改动。推送 `origin/feat/progression-save-system` 时 GitHub HTTPS 经直连及 `127.0.0.1:17891` 代理均返回 TLS unexpected EOF，远程分支与 PR 尚未更新；本地 `Docs/Temporary/`、`Mac/` 仍未跟踪。
- 2026-08-20 | 按 GitHub Release 工作流创建本地标签 `v0.1.1`，目标为当前版本；工作流会在标签推送后通过 `unity-windows-build.yml` 构建 Windows 包并创建 Release。标签与分支推送再次因 GitHub TLS unexpected EOF 失败，需网络恢复后重试。
- 2026-08-20 | 网络恢复后已成功推送 `feat/progression-save-system`（`082cd87..570b00f`）及标签 `v0.1.1` 至 origin；标签已触发 GitHub `Unity Windows Build`，该工作流完成后将创建并附带 Windows 压缩包的 Release。GitHub API 状态查询仍偶发连接失败，需在 Actions 页面确认最终结果。
- 2026-08-20 | Resolving the map PR against current main: retained the generic `ProjectBuild` Windows/Mac entry points, adopted main's corrected `Build/StandaloneWindows64` GitHub Actions paths and workspace-root release archive location, and preserved the map branch handoff history. Focused review and compilation validation pending before push.
- 2026-08-20 | Resolved and pushed the map PR conflicts as merge commit `baac694`; `dotnet build Assembly-CSharp.csproj --no-restore --disable-build-servers /p:UseSharedCompilation=false` passed with 0 errors and `git diff --check` passed. Retagged and force-pushed `v0.1.1` to the resolved commit so the corrected Windows Release workflow runs again. Existing MCP assembly-version warnings remain; `Docs/Temporary/` and `Mac/` stay untracked.
- 2026-08-20 | Fixed the map PR Static review failure by removing trailing whitespace from newly added Unity map `.meta` files and the two empty YAML asset fields. GUIDs, resource references, Prefab content, and gameplay code are unchanged; this permits CI `git diff --check` to pass so Unity compile validation can run.
