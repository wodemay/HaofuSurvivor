window.DOC_CONTENT = "# 局内进度存档\n\n## 存储方式\n\n`RunSaveStorage` 使用 PlayerPrefs 保存一个 JSON 字符串，键名为 `HaoFuSurvivor.ActiveRun`。存档只保存数值和 ID，不保存 Unity 对象引用。\n\n## 快照字段\n\n- 角色 ID、对局时间和时间阶段。\n- 玩家生命值与二维位置。\n- 等级、当前经验和升级阈值。\n- 每个 WeaponRuntime 的 Weapon ID、等级、可升级状态、Attack 列表和属性修正。\n\n## 保存时机\n\n- 运行中每 30 秒自动保存。\n- 暂停、返回主菜单、切后台和退出应用时立即保存。\n- 胜利或失败结算时清除未完成对局存档。\n\n## 恢复规则\n\n主菜单通过 `HasSavedRunQuery` 判断是否显示继续按钮。点击后由 `ContinueSavedRunCommand` 重建 PlayerRoot，再恢复快照数据。敌人、经验球、投射物和对象池实例不直接保存，会按恢复后的时间重新生成。\n\n当前为单槽位、未加密的单机开发实现；后续可增加版本号、校验、迁移和多槽位。\n";