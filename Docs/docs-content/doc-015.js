window.DOC_CONTENT = "# 技能组\n\n`CharacterConfig.SkillGroupId` 指向 `SkillGroupConfig`。\n\n技能组只定义角色开局拥有的内容：\n\n- `StartingWeaponIds`\n- `StartingSkillIds`\n- `StartingDodgeId`\n\n`RequireStartingWeapons` 决定初始 Weapon 是否属于核心启动条件，默认值为 `true`。技能组装配按槽位返回结果：Weapon 失败可阻止启动，Skill 和 Dodge 失败只记录并禁用对应能力。\n\n`PlayerSpawnSystem` 创建玩家后调用 `PlayerLoadoutSystem.EquipInitialSkillGroup()`。武器进入运行时容器，闪避通过 `Resources/Configs/Dodge/` 的独立目录按 ID 装备并创建运行时状态；技能行为仍只保存 ID 接口。闪避的执行、冷却、无敌和升级见 `Guides/DodgeSystem.zh-CN.md`。\n\n局内新增内容应由独立成长系统进入 `PlayerLoadoutSystem`，不修改角色或技能组资产。\n";