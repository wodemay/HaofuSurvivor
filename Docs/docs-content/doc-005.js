window.DOC_CONTENT = "# 闪避系统\n\n当前普通闪避独立于 Weapon 和 Attack。本文说明其配置、输入和运行时边界；角色专属闪避留待后续扩展。\n\n## 配置与装备\n\n配置位于 `Resources/Configs/Dodge/`：`DodgeCatalog.asset` 管理 ID，`Dodge_Basic.asset` 是当前 `Id = 1` 的普通闪避。`DodgeConfig` 定义冷却、持续时间、距离、无敌时间和升级增量。\n\n角色经 `SkillGroupConfig.StartingDodgeId` 装备闪避。`PlayerLoadoutSystem.SetDodge()` 调用 `DodgeSystem.Equip()` 创建 `DodgeRuntimeData`；Dodge 缺失属于可选能力失败，只清空闪避并保留对局启动。\n\n## 输入与执行\n\n`InputSystem.OnRunUpdate()` 读取方向轴，并在按下左 Shift 或右 Shift 时发送 `RequestDodgeCommand`。`DodgeSystem.TryStart()` 检查 Active、玩家状态、冷却和持续状态；无方向时使用最近方向，仍无方向则向右。\n\n开始时记录方向、持续时间和冷却，并写入独立的闪避无敌时间。普通移动在 Dodge 激活期间由 `PlayerSystem` 跳过。\n\n## 时间推进\n\n`DodgeSystem` 实现 `IRunFixedUpdateable`，只在装备成功时由 `GameLoopSystem` 注册。`OnRunFixedUpdate()` 使用统一的 `FixedDeltaTime` 递减冷却、推进冲刺，并在结束时发送事件。玩家普通无敌与闪避无敌由 `PlayerSystem.OnRunUpdate()` 统一递减。\n\n暂停、升级选择、返回和重开都会停止业务 Tick 或调用 `Reset()`，不会留下闪避位移。\n\n## 升级与扩展\n\n升级选项会包含可升级 Dodge，升级只改变 `DodgeRuntimeData`。专属闪避可通过 `ExecutorId` 扩展为独立行为；路径伤害、火焰等效果应由 System/Executor 接入统一 Tick，不能写入 `PlayerController`。\n";