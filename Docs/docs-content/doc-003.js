window.DOC_CONTENT = "# 当前游戏逻辑\n\n本文只描述已经落地的运行链路；模块设计见 `GameArchitecture.zh-CN.md`，脚本定位见 `Scripts/ScriptReference.zh-CN.md`。\n\n## 启动\n\n`GameStart.Awake()` 初始化 ResKit 和 `GameArchitecture`。`Start()` 打开主菜单。主菜单的开始按钮进入角色选择，确认角色后发送 `StartSelectedCharacterRunCommand`。\n\n## 开局\n\n`PlayerSpawnSystem.SpawnSelectedCharacter()` 创建 `PlayerRoot`，挂载角色内容、生命条、CombatEntity 和移动组件，再由 `PlayerLoadoutSystem` 装配角色的初始技能组。装配失败会销毁半初始化玩家并中止开局。\n\n`RunSystem.StartRun()` 将阶段设为 `Active`，重置计时、敌人、经验和升级队列，并发送 `RunStartedEvent`。\n\n## 对局运行\n\n`GameStart.Update()` 驱动统一时间、经验、攻击和无敌帧；`FixedUpdate()` 驱动物理时间和敌人移动。所有运行模块先检查 `RunTimerSystem.IsRunning()`。\n\n敌人由 `EnemySystem` 按时间阶段和 `EnemyConfig` 生成，`EnemyFactory` 负责 `EnemyRoot` 池化。攻击通过共享 Combat 流程处理，死亡后进入经验掉落事件。\n\n## 结束与返回\n\n玩家死亡进入 `Defeat`，打开失败结算面板。重开会清理上一局并使用当前角色创建新 PlayerRoot；返回主菜单会释放敌人、经验球、投射物和玩家。\n\n## 继续游戏\n\n主菜单根据存档查询结果决定是否提供继续入口；恢复由 `ContinueSavedRunCommand` 进入 `RunSystem`。快照字段和恢复边界见 `Docs/Guides/RunSave.zh-CN.md`。\n";