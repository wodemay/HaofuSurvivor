window.DOC_CONTENT = "# 当前游戏逻辑\r\n\r\n本文只记录已落地的运行链路；职责边界见 `GameArchitecture.zh-CN.md`，脚本定位见 `Scripts/ScriptReference.zh-CN.md`。\r\n\r\n## 启动与开局\r\n\r\n`GameStart.Awake()` 初始化 ResKit 和 `GameArchitecture`，`Start()` 打开主菜单。选角确认后，`StartSelectedCharacterRunCommand` 让 `PlayerSpawnSystem` 创建 PlayerRoot、角色内容、生命条和初始技能组；必需 Weapon 装配失败会中止开局，可选 Dodge 失败只禁用闪避。\r\n\r\n`RunSystem.StartRun()` 进入 `Active`，重置运行数据并调用 `GameLoopSystem.BeginRun()` 注册当前需要时间推进的模块。\r\n\r\n## 对局运行\r\n\r\n`GameStart.Update()` 只发送 `TickGameLoopCommand`，`FixedUpdate()` 只发送 `TickGamePhysicsCommand`。`GameLoopSystem` 先让 `RunTimerSystem` 更新逻辑时间；仅当对局处于 Active 时，才向已注册的 `IRunUpdateable` / `IRunFixedUpdateable` 分发 Tick。\r\n\r\n当前帧 Tick 包括输入、玩家无敌帧、自动攻击、经验球和自动存档；物理 Tick 包括玩家移动、敌人生成/追击、闪避和活跃投射物。暂停或升级选择会停止这两类业务 Tick。\r\n\r\n## 结束、重开与继续\r\n\r\n玩家死亡进入 `Defeat` 并打开失败结算面板。重开会结算上一局、释放运行时对象，并按当前已选角色创建新 PlayerRoot；返回主菜单也会释放敌人、经验球、投射物和玩家。继续游戏由 `ContinueSavedRunCommand` 重建玩家后恢复快照；存档详情见 `Guides/RunSave.zh-CN.md`。\r\n";